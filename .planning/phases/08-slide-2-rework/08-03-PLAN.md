---
phase: 08-slide-2-rework
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - src/app/what-is-tailwind/page.tsx
  - src/components/ButtonComparison.tsx
autonomous: true
requirements:
  - S2-01
  - S2-02
  - S2-03
  - S2-04
must_haves:
  truths:
    - "User sees the Slide 2 page with four sections in order: SlideLayout title → philosophy intro banner → 3 problem cards → two-column demo grid"
    - "User sees 'A Utility-First CSS Framework' as the philosophy intro heading at the top"
    - "User sees three problem cards (Context Switching, Naming Things Is Hard, CSS Bloat) in a 3-column grid"
    - "User sees the SemanticButton in the LEFT demo column and the UtilityButton in the RIGHT demo column"
    - "User sees the naming card (with .card-header {} / .card-title {} / .card-highlighted {} CodeCallout) in the LEFT column below SemanticButton (D-13)"
    - "User sees the existing Tailwind card + CodeCallout in the RIGHT column below UtilityButton (preserved verbatim)"
    - "ButtonComparison.tsx is deleted; no remaining import of it anywhere in the repo"
    - "yarn build passes with no purged classes; yarn test --run is fully green"
  artifacts:
    - path: src/app/what-is-tailwind/page.tsx
      provides: Reworked Slide 2 with four-section narrative arc
      contains: "SemanticButton"
    - path: src/components/ButtonComparison.tsx
      provides: (deleted — file no longer exists)
      contains: ""
  key_links:
    - from: src/app/what-is-tailwind/page.tsx
      to: src/components/SemanticButton.tsx
      via: "<SemanticButton />"
      pattern: "import SemanticButton from \"@/components/SemanticButton\""
    - from: src/app/what-is-tailwind/page.tsx
      to: src/components/UtilityButton.tsx
      via: "<UtilityButton />"
      pattern: "import UtilityButton from \"@/components/UtilityButton\""
    - from: src/app/what-is-tailwind/page.tsx
      to: NAMING_CALLOUT const → CodeCallout (single-source)
      via: "<CodeCallout classes={NAMING_CALLOUT} />"
      pattern: "classes=\\{NAMING_CALLOUT\\}"
    - from: src/app/what-is-tailwind/page.tsx
      to: CARD_CLASSES const → naming card shell + Tailwind card shell + CodeCallout (single-source)
      via: "shared const used for both card shells"
      pattern: "className=\\{CARD_CLASSES\\}"
---

<objective>
Rewrite `src/app/what-is-tailwind/page.tsx` end-to-end to deliver the four-section Slide 2 narrative arc locked in CONTEXT.md and UI-SPEC.md: (1) full-width philosophy intro banner [S2-01], (2) 3-card problem row [S2-02], (3) two-column demo grid with SemanticButton (left) + UtilityButton (right) [S2-03], (4) naming card with `.card-header {} / .card-title {} / .card-highlighted {}` CodeCallout in the LEFT column below SemanticButton [S2-04, D-13]. Delete `ButtonComparison.tsx` for hygiene now that nothing imports it.

Purpose: This is the user-visible deliverable for Phase 8. After this plan, the page meets all four ROADMAP success criteria, all four S2-XX requirements, and all 16 D-XX locked decisions.

Output: Rewritten page.tsx that turns the WhatIsTailwind.test.tsx assertions from Plan 01 green, deleted ButtonComparison.tsx, and a clean `yarn test --run && yarn build`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-slide-2-rework/08-CONTEXT.md
@.planning/phases/08-slide-2-rework/08-RESEARCH.md
@.planning/phases/08-slide-2-rework/08-PATTERNS.md
@.planning/phases/08-slide-2-rework/08-UI-SPEC.md

@CLAUDE.md
@src/app/what-is-tailwind/page.tsx
@src/components/SlideLayout.tsx
@src/components/CodeCallout.tsx

<interfaces>
<!-- Existing const reused verbatim. Do NOT modify. -->

```typescript
// SOURCE: current src/app/what-is-tailwind/page.tsx lines 5-6
const CARD_CLASSES =
  "bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 rounded-2xl p-6 shadow-sm 3xl:p-10";
```

<!-- New const introduced in this plan. -->

```typescript
// NEW: introduced by this plan in src/app/what-is-tailwind/page.tsx
const NAMING_CALLOUT = `.card-header {}\n.card-title {}\n.card-highlighted {}`;
```

<!-- Components introduced by Plan 02. -->

```typescript
// SOURCE: src/components/SemanticButton.tsx — created by Plan 02
export default function SemanticButton(): JSX.Element;  // "use client" island

// SOURCE: src/components/UtilityButton.tsx — created by Plan 02
export default function UtilityButton(): JSX.Element;  // RSC
```

<!-- SlideLayout API (existing — already in use). -->

```typescript
// SOURCE: src/components/SlideLayout.tsx
interface SlideLayoutProps {
  number: string;
  title: string;
  children?: React.ReactNode;
}
```

<!-- AUTHORITATIVE COPY (UI-SPEC §Copywriting Contract — use these strings verbatim) -->
| Element | Copy |
|---------|------|
| Philosophy intro heading | "A Utility-First CSS Framework" |
| Philosophy intro body line 1 | "Tailwind gives you composable, single-purpose utility classes that you apply directly in your markup." |
| Philosophy intro body line 2 | "No context-switching. No naming things. No growing stylesheets." |
| Left column overline | "Semantic CSS" |
| Left column caption | "The class name hides the implementation." |
| Right column overline | "Utility-First Tailwind" |
| Right column caption | "Every style is visible inline." |
| Problem card 1 heading | "Context Switching" |
| Problem card 1 body | "You write HTML, jump to a CSS file, write a rule, jump back. Every change lives in two places." |
| Problem card 2 heading | "Naming Things Is Hard" |
| Problem card 2 body | "What do you call the highlighted card? `.featured`? `.card--active`? `.highlight-wrapper`? You decide—forever." |
| Problem card 3 heading | "CSS Bloat" |
| Problem card 3 body | "Old rules never safely delete. Stylesheets grow. Nobody knows what's still in use." |
| Naming card overline | "With Semantic CSS" |
| Naming card heading | "Card Component" |
| Naming card body | "The same component would require you to coin these class names:" |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite page.tsx with four-section layout (intro → problem cards → demo grid → ButtonComparison removed)</name>
  <files>src/app/what-is-tailwind/page.tsx</files>
  <read_first>
    - src/app/what-is-tailwind/page.tsx (current state — preserves CARD_CLASSES, the right-column Tailwind card markup, captions; rewrites the rest)
    - src/test/app/WhatIsTailwind.test.tsx (Plan 01 RED tests — this task must turn ALL of them green)
    - src/test/app/slide-pages.test.tsx (existing smoke test — must remain green; renders WhatIsTailwind and asserts slide number "02" + heading "What is Tailwind?")
    - .planning/phases/08-slide-2-rework/08-CONTEXT.md (D-01 through D-16 — every decision applies to this file)
    - .planning/phases/08-slide-2-rework/08-UI-SPEC.md §Page Layout Contract and §Copywriting Contract
    - .planning/phases/08-slide-2-rework/08-PATTERNS.md (lines 24-124 — exact section assembly pattern)
    - .planning/phases/08-slide-2-rework/08-RESEARCH.md (Pitfalls 1, 5 — naming card MUST be in left column; page.tsx MUST stay RSC)
    - src/components/SlideLayout.tsx (wrapper API — `number="02"` `title="What is Tailwind?"` preserved verbatim)
  </read_first>
  <behavior>
    - WhatIsTailwind page test 1: renders slide number "02"
    - WhatIsTailwind page test 2: renders heading `/A Utility-First CSS Framework/i` (S2-01)
    - WhatIsTailwind page test 3: body contains `/composable/i` AND `/utility classes/i` (S2-01)
    - WhatIsTailwind page test 4: three problem-card headings present — `Context Switching`, `Naming Things Is Hard`, `CSS Bloat` (S2-02)
    - WhatIsTailwind page test 5: both column overlines present — `Semantic CSS`, `Utility-First Tailwind` (S2-03)
    - WhatIsTailwind page test 6: exactly two demo buttons named `Button` (one from SemanticButton, one from UtilityButton) (S2-03)
    - WhatIsTailwind page test 7: naming callout regex `/\.card-header\s*\{\}[\s\S]*\.card-title\s*\{\}[\s\S]*\.card-highlighted\s*\{\}/` matches (S2-04)
    - WhatIsTailwind page test 8: total `Button`-named buttons ≤ 2 (regression guard against ButtonComparison)
  </behavior>
  <action>
Replace the entire contents of `src/app/what-is-tailwind/page.tsx` with the implementation below. NO `"use client"` directive (Pitfall 5). All copy is verbatim from the UI-SPEC §Copywriting Contract embedded in `<interfaces>` above. All structural class strings are verbatim from PATTERNS.md and CARD_CLASSES is preserved unchanged.

```tsx
import SlideLayout from "@/components/SlideLayout";
import CodeCallout from "@/components/CodeCallout";
import SemanticButton from "@/components/SemanticButton";
import UtilityButton from "@/components/UtilityButton";

const CARD_CLASSES =
  "bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 rounded-2xl p-6 shadow-sm 3xl:p-10 4xl:p-14";

const NAMING_CALLOUT = `.card-header {}\n.card-title {}\n.card-highlighted {}`;

export default function WhatIsTailwind() {
  return (
    <SlideLayout number="02" title="What is Tailwind?">
      {/* ══ Section 1: Philosophy Intro Banner (S2-01) ══ */}
      <section>
        <h2 className="text-3xl font-bold text-slate-900 dark:text-white 3xl:text-5xl 4xl:text-6xl">
          A Utility-First CSS Framework
        </h2>
        <p className="mt-4 text-sm leading-relaxed text-slate-600 dark:text-slate-400 3xl:text-xl 4xl:text-2xl 3xl:mt-6 4xl:mt-8">
          Tailwind gives you composable, single-purpose utility classes that you
          apply directly in your markup.
        </p>
        <p className="mt-2 text-sm leading-relaxed text-slate-600 dark:text-slate-400 3xl:text-xl 4xl:text-2xl 3xl:mt-4 4xl:mt-5">
          No context-switching. No naming things. No growing stylesheets.
        </p>
      </section>

      {/* Section spacer */}
      <div className="mt-8 3xl:mt-16 4xl:mt-24" />

      {/* ══ Section 2: Problem Cards (S2-02) ══ */}
      <section>
        <div className="grid grid-cols-3 gap-6 3xl:gap-12 4xl:gap-16">
          <div className={CARD_CLASSES}>
            <h3 className="text-base font-semibold text-slate-900 dark:text-white 3xl:text-xl 4xl:text-2xl">
              Context Switching
            </h3>
            <p className="mt-2 text-sm text-slate-600 dark:text-slate-400 3xl:text-base 4xl:text-lg 3xl:mt-3 4xl:mt-4">
              You write HTML, jump to a CSS file, write a rule, jump back. Every
              change lives in two places.
            </p>
          </div>
          <div className={CARD_CLASSES}>
            <h3 className="text-base font-semibold text-slate-900 dark:text-white 3xl:text-xl 4xl:text-2xl">
              Naming Things Is Hard
            </h3>
            <p className="mt-2 text-sm text-slate-600 dark:text-slate-400 3xl:text-base 4xl:text-lg 3xl:mt-3 4xl:mt-4">
              What do you call the highlighted card? <code>.featured</code>?{" "}
              <code>.card--active</code>? <code>.highlight-wrapper</code>? You
              decide—forever.
            </p>
          </div>
          <div className={CARD_CLASSES}>
            <h3 className="text-base font-semibold text-slate-900 dark:text-white 3xl:text-xl 4xl:text-2xl">
              CSS Bloat
            </h3>
            <p className="mt-2 text-sm text-slate-600 dark:text-slate-400 3xl:text-base 4xl:text-lg 3xl:mt-3 4xl:mt-4">
              Old rules never safely delete. Stylesheets grow. Nobody knows
              what&apos;s still in use.
            </p>
          </div>
        </div>
      </section>

      {/* Section spacer */}
      <div className="mt-16 3xl:mt-24 4xl:mt-32" />

      {/* ══ Section 3, Row 1: Button demos — stacks on small screens (S2-03) ══ */}
      <div className="grid grid-cols-1 md:grid-cols-2 gap-6 3xl:gap-12 4xl:gap-16">
        {/* Left: Semantic CSS button */}
        <div>
          <p className="text-xs font-semibold tracking-widest uppercase text-cyan-600 dark:text-cyan-400 3xl:text-base 4xl:text-lg">
            Semantic CSS
          </p>
          <p className="mt-2 text-sm leading-relaxed text-slate-600 dark:text-slate-400 3xl:text-xl 4xl:text-2xl 3xl:mt-4 4xl:mt-5">
            The class name hides the implementation.
          </p>
          <div className="mt-4 3xl:mt-8 4xl:mt-10">
            <SemanticButton />
          </div>
        </div>

        {/* Right: Utility-First Tailwind button */}
        <div>
          <p className="text-xs font-semibold tracking-widest uppercase text-cyan-600 dark:text-cyan-400 3xl:text-base 4xl:text-lg">
            Utility-First Tailwind
          </p>
          <p className="mt-2 text-sm leading-relaxed text-slate-600 dark:text-slate-400 3xl:text-xl 4xl:text-2xl 3xl:mt-4 4xl:mt-5">
            Every style is visible inline.
          </p>
          <div className="mt-4 3xl:mt-8 4xl:mt-10">
            <UtilityButton />
          </div>
        </div>
      </div>

      {/* Row spacer */}
      <div className="mt-10 3xl:mt-14 4xl:mt-20" />

      {/* ══ Section 3, Row 2: Card components — stacks on small screens (S2-04) ══ */}
      <div className="grid grid-cols-1 md:grid-cols-2 gap-6 3xl:gap-12 4xl:gap-16">
        {/* Left: Naming card — D-13 locks this to the left column */}
        <div>
          <p className="text-xs font-semibold tracking-widest uppercase text-cyan-600 dark:text-cyan-400 3xl:text-base 4xl:text-lg">
            Semantic CSS
          </p>
          <div className={`${CARD_CLASSES} mt-2 3xl:mt-4 4xl:mt-5`}>
            <h3 className="text-base font-semibold text-slate-900 dark:text-white 3xl:text-xl 4xl:text-2xl">
              Card Component
            </h3>
            <p className="mt-2 text-sm text-slate-600 dark:text-slate-400 3xl:text-base 4xl:text-lg 3xl:mt-3 4xl:mt-4">
              The same component would require you to coin these class names:
            </p>
          </div>
          <div className="mt-4 3xl:mt-8 4xl:mt-10">
            <CodeCallout classes={NAMING_CALLOUT} />
          </div>
        </div>

        {/* Right: Tailwind card */}
        <div>
          <p className="text-xs font-semibold tracking-widest uppercase text-cyan-600 dark:text-cyan-400 3xl:text-base 4xl:text-lg">
            Utility-First Tailwind
          </p>
          <div className={`${CARD_CLASSES} mt-2 3xl:mt-4 4xl:mt-5`}>
            <h3 className="text-base font-semibold text-slate-900 dark:text-white 3xl:text-xl 4xl:text-2xl">
              Card Component
            </h3>
            <p className="mt-2 text-sm text-slate-600 dark:text-slate-400 3xl:text-base 4xl:text-lg 3xl:mt-3 4xl:mt-4">
              This card&apos;s entire visual style is declared right here in
              its class list. No separate stylesheet needed.
            </p>
          </div>
          <div className="mt-4 3xl:mt-8 4xl:mt-10">
            <CodeCallout classes={CARD_CLASSES} />
          </div>
        </div>
      </div>
    </SlideLayout>
  );
}
```

Important structural rules (from CONTEXT.md / RESEARCH.md):
1. NO `"use client"` directive (Pitfall 5)
2. Naming card lives in the LEFT column inside the `Section 3` grid (D-13, Pitfall 1) — the right column ends with the Tailwind card + CARD_CLASSES CodeCallout
3. Both card shells reuse the `CARD_CLASSES` const verbatim — single-source pattern (D-15, D-16)
4. Section spacers between Sections 1↔2 and 2↔3 use `mt-16 3xl:mt-24` (D-01, D-03)
5. NO border dividers between sections — spacing only (D-03)
6. Three problem cards in `grid grid-cols-3 gap-6 3xl:gap-12` (D-05)
7. Demo grid is `grid grid-cols-2 gap-6 3xl:gap-12` (preserved from current page)
8. Overlines use the canonical class string verbatim from UI-SPEC (matches Phase 2 pattern)
9. The right-column Tailwind card markup (heading "Card Component", body about declaring style in class list) is PRESERVED from the current page — do NOT change its text or class strings; the only insertion in the right column is `<UtilityButton />` above it
10. Replace the use of apostrophes in JSX text with `&apos;` (matches existing convention on line 39 of current page) — the body for problem card 3 must use `what&apos;s` to avoid lint errors
11. Use `<code>` elements (not template-string interpolation) for the inline class names in the "Naming Things Is Hard" card body — keeps Tailwind class detection clean

Forbidden patterns:
- No `"use client"` — `grep -c "use client" src/app/what-is-tailwind/page.tsx` must return `0`
- No `import ButtonComparison` — `grep -c ButtonComparison src/app/what-is-tailwind/page.tsx` must return `0`
- No template literal class interpolation like `` `bg-${...}` `` — single static strings only (CLAUDE.md)
- No `useState`, `useEffect`, `useRef` — RSC has no hooks
  </action>
  <verify>
    <automated>yarn test --run src/test/app/WhatIsTailwind.test.tsx src/test/app/slide-pages.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/what-is-tailwind/page.tsx` exists and was modified (line count ≥ 90)
    - `grep -c "use client" src/app/what-is-tailwind/page.tsx` returns `0`
    - `grep -c ButtonComparison src/app/what-is-tailwind/page.tsx` returns `0`
    - Contains exact string `import SemanticButton from "@/components/SemanticButton"`
    - Contains exact string `import UtilityButton from "@/components/UtilityButton"`
    - Contains exact string `const NAMING_CALLOUT = \`.card-header {}\n.card-title {}\n.card-highlighted {}\``
    - Contains exact string `<SemanticButton />`
    - Contains exact string `<UtilityButton />`
    - Contains exact string `A Utility-First CSS Framework`
    - Contains exact string `Context Switching`
    - Contains exact string `Naming Things Is Hard`
    - Contains exact string `CSS Bloat`
    - Contains exact string `With Semantic CSS`
    - Contains exact string `<CodeCallout classes={NAMING_CALLOUT} />`
    - Contains exact string `<CodeCallout classes={CARD_CLASSES} />`
    - Contains exact substring `grid grid-cols-3 gap-6 3xl:gap-12`
    - Contains exact substring `grid grid-cols-2 gap-6 3xl:gap-12`
    - Contains at least two `<div className="mt-16 3xl:mt-24" />` section spacers
    - `yarn test --run src/test/app/WhatIsTailwind.test.tsx` exits 0 (all 8 assertions green)
    - `yarn test --run src/test/app/slide-pages.test.tsx` exits 0 (existing smoke test still passes)
  </acceptance_criteria>
  <done>page.tsx is rewritten as a four-section RSC: philosophy intro → problem cards → two-column demo grid (with naming card in LEFT column under SemanticButton; Tailwind card preserved in RIGHT column under UtilityButton). All Plan 01 page-level tests are green. Slide-pages smoke test still green. No "use client", no ButtonComparison reference.</done>
</task>

<task type="auto">
  <name>Task 2: Delete ButtonComparison.tsx and verify no remaining imports</name>
  <files>src/components/ButtonComparison.tsx</files>
  <read_first>
    - src/components/ButtonComparison.tsx (file being deleted — confirm no recent local edits exist)
    - src/app/what-is-tailwind/page.tsx (just-rewritten Task 1 output — must NOT import ButtonComparison)
    - .planning/phases/08-slide-2-rework/08-RESEARCH.md (§Open Questions Q2 — recommended deletion for hygiene)
    - .planning/phases/08-slide-2-rework/08-UI-SPEC.md (§Component Inventory — ButtonComparison status: "delete after rework")
  </read_first>
  <action>
Delete the file `src/components/ButtonComparison.tsx`. The file is no longer referenced after Task 1 removes its import from page.tsx.

Use the standard Bash tool `rm` to delete:

```bash
rm src/components/ButtonComparison.tsx
```

After deletion, verify NO file in the repository imports it:

```bash
grep -r "ButtonComparison" src/ --include="*.ts" --include="*.tsx" 2>&1 || true
grep -r "ButtonComparison" .planning/ 2>&1 || true  # Allow planning doc references
```

The `src/` grep MUST return zero matches. The `.planning/` grep is allowed to return matches (planning artifacts reference the deleted file by name; that is documentation, not code).

If `src/` grep finds a remaining import:
- The most likely cause is Task 1 left `import ButtonComparison from "@/components/ButtonComparison"` at the top of page.tsx — re-read Task 1's action and remove the line
- A test file may reference ButtonComparison; check `src/test/` and either delete that test file (if it exists) or revert this deletion until the test is removed in a follow-up
  </action>
  <verify>
    <automated>test ! -f src/components/ButtonComparison.tsx && grep -r "ButtonComparison" src/ --include="*.ts" --include="*.tsx" 2>&1 | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/ButtonComparison.tsx` does NOT exist (`test ! -f` exits 0)
    - `grep -r "ButtonComparison" src/ --include="*.ts" --include="*.tsx"` returns ZERO matches
    - `yarn test --run` still exits 0 (no test imported ButtonComparison)
    - `yarn build` still exits 0
  </acceptance_criteria>
  <done>ButtonComparison.tsx is removed from src/components/. No source file in src/ references it. yarn test and yarn build remain green.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — full suite green and Wave 0 tests all flipped</name>
  <files>(none modified — verification gate)</files>
  <read_first>
    - .planning/phases/08-slide-2-rework/08-VALIDATION.md (Per-Task Verification Map — all six rows must be green)
    - .planning/ROADMAP.md §Phase 8 Success Criteria (4 items)
    - .planning/REQUIREMENTS.md S2-01 through S2-04
  </read_first>
  <action>
Run the full suite and confirm everything is green:

```bash
yarn test --run
yarn build
```

Cross-check against the Phase 8 ROADMAP success criteria — each must be observable in the rendered DOM:

1. SC-1 — "User sees a philosophy intro section at the top of Slide 2 with a 'utility-first CSS framework' definition before any demo content"
   → Verified by `WhatIsTailwind.test.tsx` heading + body assertions
2. SC-2 — "User sees 2–3 condensed problem cards (context-switching, naming things is hard, CSS bloat) above the interactive demo — legible at 1920px"
   → Verified by `WhatIsTailwind.test.tsx` three-heading assertion. (1920px legibility = manual; covered by 08-VALIDATION.md Manual-Only table.)
3. SC-3 — "User sees the semantic button demo and utility button demo side-by-side in a two-column grid, one per column"
   → Verified by `WhatIsTailwind.test.tsx` two-button + overline assertion + `grid grid-cols-2 gap-6 3xl:gap-12` literal in page.tsx
4. SC-4 — "User sees a second card below the existing Tailwind card demo with a CodeCallout showing `.card-header {}`, `.card-title {}`, `.card-highlighted {}` class names"
   → Verified by `WhatIsTailwind.test.tsx` naming-callout regex. NOTE: D-13 places this card in the LEFT column despite SC-4's "below the existing Tailwind card" phrasing — the user explicitly relocated it during /gsd-discuss-phase; this is the intended behavior.

If `yarn test` or `yarn build` fails, return to the offending task. If a SC item is unverifiable, the test scaffold is incomplete — do not mark the phase done.

Confirm zero references to `ButtonComparison` in any `src/**` file. Confirm `"use client"` appears only in `src/components/SemanticButton.tsx` and other pre-existing client islands (NOT in page.tsx, NOT in UtilityButton.tsx).
  </action>
  <verify>
    <automated>yarn test --run && yarn build && ! grep -r "ButtonComparison" src/ --include="*.ts" --include="*.tsx" && ! grep -l "use client" src/app/what-is-tailwind/page.tsx src/components/UtilityButton.tsx 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `yarn test --run` exits 0
    - `yarn build` exits 0
    - All 8 WhatIsTailwind.test.tsx assertions pass
    - All 3 SemanticButton.test.tsx assertions pass (still green from Plan 02)
    - All 3 UtilityButton.test.tsx assertions pass (still green from Plan 02)
    - All pre-existing tests pass (slide-pages, ConditionalPanels, history-of-css, etc.)
    - `grep -r "ButtonComparison" src/ --include="*.ts" --include="*.tsx"` returns zero matches
    - `grep -l "use client" src/app/what-is-tailwind/page.tsx` returns nothing (file does not contain it)
    - `grep -l "use client" src/components/UtilityButton.tsx` returns nothing
    - `grep -l "use client" src/components/SemanticButton.tsx` returns the path (file DOES contain it — D-09)
  </acceptance_criteria>
  <done>Full Phase 8 suite green. All four ROADMAP success criteria satisfied. ButtonComparison.tsx fully retired from src/. "use client" boundary correctly placed at SemanticButton only. Phase ready for /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static page render → DOM | All content is hardcoded in page.tsx; no user input, no async data fetching, no rehydration of untrusted state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Tampering (XSS via className) | page.tsx | mitigate | All `className` values are static string literals or named consts (CARD_CLASSES, NAMING_CALLOUT). No template-string interpolation in any className. CLAUDE.md prohibits dynamic class strings; acceptance criteria enforce zero `${...}` inside className expressions. |
| T-08-08 | Tampering (XSS via children/innerHTML) | page.tsx, CodeCallout | mitigate | NAMING_CALLOUT and all body copy are static template/string literals authored at compile time. Page does NOT use `dangerouslySetInnerHTML`. CodeCallout renders its `classes` prop as a text node (verified in src/components/CodeCallout.tsx — `<p>{classes}</p>`), preserving React's automatic escaping. |
| T-08-09 | Information Disclosure | page.tsx | accept | Page contains no user data, no PII, no environment variables, no secrets. All copy is public marketing/teaching content for a static presentation. |
| T-08-10 | Tampering (link/route hijack) | SlideLayout (existing — unchanged this phase) | accept | SlideLayout's back-to-overview Link uses a hardcoded `href="/"`; not modified by this plan. Existing test in src/test/app/history-of-css/page.test.tsx asserts the hardcoded route, providing regression coverage. |
| T-08-11 | Repudiation / Audit | page.tsx | accept | No actions taken on the page (D-08 removes the only click handler that existed). No audit log relevant. |
</threat_model>

<verification>
After all three tasks:

```bash
yarn test --run
yarn build
grep -r "ButtonComparison" src/ --include="*.ts" --include="*.tsx"
ls src/components/ButtonComparison.tsx 2>&1
```

Expected:
- All tests green (Plan 01 + Plan 02 + pre-existing)
- Build clean
- No grep matches for ButtonComparison in src/
- `ls` reports "No such file or directory" for ButtonComparison.tsx
</verification>

<success_criteria>
- All four S2-XX requirements observable via passing tests
- All four ROADMAP §Phase 8 Success Criteria observable via passing tests (manual 1920px verification deferred to /gsd-verify-work)
- All 16 D-XX locked decisions implemented (verified by file content + acceptance criteria)
- ButtonComparison.tsx deleted; zero references in src/
- "use client" present at SemanticButton.tsx only — not page.tsx, not UtilityButton.tsx
- yarn test --run && yarn build both green
</success_criteria>

<output>
After completion, create `.planning/phases/08-slide-2-rework/08-03-SUMMARY.md` with:
- Final state of page.tsx (line count, sections rendered)
- Confirmation ButtonComparison.tsx is deleted
- Test count delta: tests added by Plan 01 — all green now
- Build status
- Any deviations from the action and the reason
- Manual-verification items deferred to /gsd-verify-work (1920px legibility, dark mode visual check — per 08-VALIDATION.md)
</output>
