---
context: phase
phase: 01-shared-infrastructure
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-04-29T16:40:44.375Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints were encountered this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Dynamic class interpolation | Never construct Tailwind classes via template literals or string concatenation — purger cannot detect them | advisory | CodeCallout chip class string MUST be a single static literal in source |

<current_state>
Phase 1 Wave 1 is fully complete. Plan 03 (cn() + CodeCallout) is partially done:
- Task 1 (src/lib/utils.ts): DONE — rescued from an interrupted worktree and merged to main (commit 8aa09ee)
- Task 2 (src/components/CodeCallout.tsx): NOT STARTED — still needs to be created

The only remaining work in Phase 1 is creating the CodeCallout RSC. No 01-03-SUMMARY.md exists yet, so `/gsd-execute-phase 1` will detect plan 03 as incomplete and dispatch a fresh executor for it.
</current_state>

<completed_work>

**Plan 01-01: Install shared runtime libraries** ✓
- clsx@2.1.1, tailwind-merge@3.5.0, motion@12.38.0, shiki@4.0.2, shiki-magic-move@1.3.0 installed as runtime deps
- Commits: 616a341 (feat), 19b0443 (docs)

**Plan 01-02: Remove prefers-color-scheme from globals.css** ✓
- 6-line @media (prefers-color-scheme: dark) block deleted
- @custom-variant dark is now sole dark mode source of truth
- Commits: ca383d5 (fix), eb71424 (docs worktree)

**Plan 01-03, Task 1: Create cn() utility at src/lib/utils.ts** ✓
- File created with exact locked implementation (clsx + twMerge, variadic ClassValue)
- Commit: 8aa09ee (rescued from interrupted worktree)

**Post-Wave-1 test gate**: All 55 tests passed.
</completed_work>

<remaining_work>

**Plan 01-03, Task 2: Create CodeCallout RSC at src/components/CodeCallout.tsx** ← START HERE

The full spec is in `.planning/phases/01-shared-infrastructure/01-03-PLAN.md` Task 2.

Exact implementation (locked by UI-SPEC and PATTERNS.md):

```tsx
interface CodeCalloutProps {
  classes: string;
}

export default function CodeCallout({ classes }: Readonly<CodeCalloutProps>) {
  return (
    <p className="inline-block font-mono text-[13px] leading-relaxed bg-slate-100 text-slate-700 border border-slate-200 rounded-lg px-3 py-1 whitespace-pre-wrap break-all dark:bg-slate-800 dark:text-slate-300 dark:border-slate-700 3xl:text-base 3xl:px-4 3xl:py-2 3xl:rounded-xl">
      {classes}
    </p>
  );
}
```

Key constraints:
- NO "use client" directive (RSC)
- NO cn() call (class string is static literal)
- NO template literals with ${}
- Single <p> element, single `classes: string` prop
- class string is one complete static literal (all classes must be in it for Tailwind purger)

After creating CodeCallout.tsx, run verification checks from the plan, then create 01-03-SUMMARY.md.
</remaining_work>

<decisions_made>

- tailwind-merge must be v3 (D-12) — v2 is Tailwind v3-only; this project uses Tailwind v4
- Install `motion` package, not `framer-motion` (D-13) — imported as "motion/react"
- @custom-variant dark is sole dark mode source of truth (D-10) — prefers-color-scheme removed
- cn() is a named export, not default export, at exactly src/lib/utils.ts (D-08, D-09)
- CodeCallout has no props other than `classes: string` — fixed appearance, no variants (D-05, D-06)
- 3xl: escalations are mandatory on CodeCallout (CLAUDE.md requirement for TV readability)
</decisions_made>

<blockers>
None — no blockers. Usage reset mid-execution; worktree was rescued cleanly.
</blockers>

## Required Reading (in order)

1. `.planning/phases/01-shared-infrastructure/01-03-PLAN.md` — full task spec for CodeCallout RSC (Task 2)
2. `.planning/phases/01-shared-infrastructure/01-UI-SPEC.md` — locked class string and rendered structure
3. `.planning/phases/01-shared-infrastructure/01-PATTERNS.md` — CodeCallout target structure and Readonly props pattern
4. `src/components/NavCard.tsx` — closest analog for RSC structure
5. `src/app/globals.css` — confirms @custom-variant dark is present (dark: classes will resolve)

## Infrastructure State

- Dev server: not running (stopped after yarn dev smoke test during Plan 01)
- Worktrees: none active (Plan 03 partial worktree cleaned up)
- Git: main branch, 18 commits ahead of origin

<context>
Wave 1 completed cleanly via parallel worktrees. Wave 2 started but the Plan 03 executor was interrupted mid-task by usage limits. utils.ts was already written correctly in the worktree before the interruption — it was rescued, committed, and merged. CodeCallout.tsx is the only file left to create for Phase 1 to be complete.
</context>

<next_action>
Start with: `/gsd-execute-phase 1` — it will detect 01-03-SUMMARY.md is missing, skip plans 01-01 and 01-02 (have SUMMARYs), and dispatch a fresh executor for Plan 03. The executor will read the plan, see utils.ts already exists (Task 1 done), and create CodeCallout.tsx (Task 2).
</next_action>
