---
phase: 01-shared-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - yarn.lock
autonomous: true
requirements:
  - INFRA-02
user_setup: []

must_haves:
  truths:
    - "yarn add succeeds without peer-dependency errors"
    - "package.json declares clsx, tailwind-merge ^3, motion, shiki, shiki-magic-move as runtime dependencies"
    - "yarn dev starts the app without module resolution errors"
    - "tailwind-merge is v3.x (not v2.x)"
  artifacts:
    - path: "package.json"
      provides: "Runtime dependencies for cn() utility, future Shiki rendering, and motion animations"
      contains: "\"clsx\""
    - path: "yarn.lock"
      provides: "Locked dependency tree for reproducible installs"
      contains: "tailwind-merge"
  key_links:
    - from: "package.json"
      to: "node_modules/tailwind-merge"
      via: "yarn install"
      pattern: "\"tailwind-merge\":\\s*\"\\^?3"
    - from: "package.json"
      to: "node_modules/motion"
      via: "yarn install"
      pattern: "\"motion\":"
---

<objective>
Install the shared runtime libraries Phase 1 and all subsequent slide phases depend on: `clsx` + `tailwind-merge` (for cn()), `motion` (for animations in later phases), `shiki` + `shiki-magic-move` (for Slide 5 syntax highlighting). Single install pass.

Purpose: Plan 02 (cn() + CodeCallout) cannot proceed until `clsx` and `tailwind-merge@^3` are resolvable. Later phases also need `motion` and `shiki` available without re-running install. Per D-11, all roadmap packages are installed in Phase 1.

Output: `package.json` and `yarn.lock` updated with five new runtime dependencies. App starts cleanly with `yarn dev`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-infrastructure/01-CONTEXT.md
@.planning/phases/01-shared-infrastructure/01-PATTERNS.md

# Current dependency state
@package.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install shared runtime libraries via yarn add</name>
  <files>package.json, yarn.lock</files>
  <read_first>
    - package.json (current dependencies state — confirms clsx, tailwind-merge, motion, shiki, shiki-magic-move are NOT yet present)
    - .planning/phases/01-shared-infrastructure/01-CONTEXT.md §Package Installation (D-11, D-12, D-13, D-14)
    - .planning/phases/01-shared-infrastructure/01-PATTERNS.md §package.json (install command and version constraints)
  </read_first>
  <action>
    Run the exact install command (per D-11 single install pass, D-12 tailwind-merge must be v3, D-13 motion package not framer-motion, D-14 shiki-magic-move installed now):

    ```bash
    yarn add clsx tailwind-merge@^3 motion shiki shiki-magic-move
    ```

    Do NOT run `yarn add framer-motion` — D-13 explicitly requires the `motion` package (imported from `"motion/react"` in later phases).
    Do NOT pin `tailwind-merge` to `^2` — D-12 requires v3 because v2 is Tailwind v3-only and this project is on Tailwind v4.
    Do NOT install any of these as devDependencies — all five are runtime dependencies (used in production code paths in later phases).

    After install completes, run `yarn dev` once to confirm the app starts without module resolution errors. Stop the dev server (Ctrl+C) once the "Ready in" message appears.
  </action>
  <verify>
    <automated>grep -q '"clsx"' package.json && grep -qE '"tailwind-merge":\s*"\^?3' package.json && grep -q '"motion"' package.json && grep -q '"shiki"' package.json && grep -q '"shiki-magic-move"' package.json && yarn install --check-files</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains the literal string `"clsx"` inside the `"dependencies"` object
    - package.json contains a tailwind-merge entry matching the regex `"tailwind-merge":\s*"\^?3` (v3 only — fails for v2)
    - package.json contains the literal string `"motion"` (NOT `"framer-motion"`)
    - package.json contains the literal string `"shiki"`
    - package.json contains the literal string `"shiki-magic-move"`
    - All five packages appear in `"dependencies"` (NOT in `"devDependencies"`)
    - `yarn install --check-files` exits 0 (no missing files, no peer-dep failures)
    - yarn.lock contains entries for all five new packages
  </acceptance_criteria>
  <done>Five runtime packages installed at correct versions, no peer-dep errors, yarn dev confirms app boots.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → local node_modules | npm/yarn registry is a third-party supply chain; installed code runs at build and runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | yarn.lock supply chain | accept | Use yarn.lock pinning; install only well-known MIT packages (clsx, tailwind-merge, motion, shiki, shiki-magic-move). No post-install scripts from unknown vendors. Static presentation app — no PII, no auth, no network endpoints to compromise. |
| T-01-02 | Information Disclosure | package.json | accept | package.json contains no secrets — only public dependency names and versions. Tracked in git. |
</threat_model>

<verification>
- `package.json` shows all five new entries in `"dependencies"`
- `yarn install` runs cleanly with no peer-dep warnings escalating to errors
- `yarn dev` starts the app and prints `"Ready in"` without module errors
</verification>

<success_criteria>
- INFRA-02 prerequisite met: `clsx` and `tailwind-merge@^3` are resolvable so Plan 02 can author `src/lib/utils.ts`
- D-11 satisfied: single install pass, all five packages in
- D-12 satisfied: tailwind-merge is v3
- D-13 satisfied: `motion` package is installed (not `framer-motion`)
- D-14 satisfied: `shiki-magic-move` is in tree even though v2-deferred for usage
</success_criteria>

<output>
After completion, create `.planning/phases/01-shared-infrastructure/01-01-SUMMARY.md`
</output>
