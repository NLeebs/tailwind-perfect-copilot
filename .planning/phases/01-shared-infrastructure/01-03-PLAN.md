---
phase: 01-shared-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/lib/utils.ts
  - src/components/CodeCallout.tsx
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02

must_haves:
  truths:
    - "cn(...inputs) is importable from '@/lib/utils' and merges conflicting Tailwind classes (twMerge + clsx)"
    - "CodeCallout is importable as a default export from '@/components/CodeCallout'"
    - "Calling <CodeCallout classes='bg-sky-500 rounded-lg px-4 py-2' /> renders a single element containing exactly that class string as its text"
    - "CodeCallout has no 'use client' directive (it is an RSC)"
    - "CodeCallout has no props other than `classes: string`"
    - "CodeCallout chip class string is a single complete static literal — no template interpolation, no dynamic concatenation"
    - "CodeCallout chip wraps long class strings without truncation (break-all + whitespace-pre-wrap)"
    - "CodeCallout chip uses dark: prefixes (not prefers-color-scheme) for dark-mode styling"
    - "CodeCallout chip declares 3xl: escalations on text size, padding, and border radius"
  artifacts:
    - path: "src/lib/utils.ts"
      provides: "cn() utility — clsx + tailwind-merge wrapper for conditional class composition"
      exports: ["cn"]
      min_lines: 5
    - path: "src/components/CodeCallout.tsx"
      provides: "RSC chip component that renders a Tailwind class string adjacent to demo output, TV-legible at 1920px"
      exports: ["default"]
      min_lines: 10
  key_links:
    - from: "src/lib/utils.ts"
      to: "node_modules/clsx, node_modules/tailwind-merge"
      via: "import { clsx, type ClassValue } from 'clsx'; import { twMerge } from 'tailwind-merge'"
      pattern: "import.*from\\s+['\"](clsx|tailwind-merge)['\"]"
    - from: "src/components/CodeCallout.tsx"
      to: "@custom-variant dark in globals.css"
      via: "dark: prefix classes (resolved by class-based variant on <html>)"
      pattern: "dark:bg-slate-800"
---

<objective>
Create the two new shared primitives every Phase 2–6 slide depends on:

1. `src/lib/utils.ts` — exports `cn(...inputs: ClassValue[]): string` — a standard `clsx` + `tailwind-merge` wrapper (D-08, D-09).
2. `src/components/CodeCallout.tsx` — an RSC chip component with a single `classes: string` prop (D-01 through D-07, INFRA-01).

Purpose: Plans 02–06 of subsequent phases will `import { cn } from "@/lib/utils"` for state-driven class merging and `import CodeCallout from "@/components/CodeCallout"` adjacent to every demo element. Both primitives must be in place before any slide work begins.

Output: Two new files. No other code changes. After this plan, the app still builds and the home page still renders.

Wave 2: depends on Plan 01 (clsx and tailwind-merge must be installed before utils.ts can resolve its imports). Independent of Plan 02 (globals.css edit) — they can both be Wave 1 ancestors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-infrastructure/01-CONTEXT.md
@.planning/phases/01-shared-infrastructure/01-PATTERNS.md
@.planning/phases/01-shared-infrastructure/01-UI-SPEC.md

# Closest analog for CodeCallout (RSC, typed props, pure display)
@src/components/NavCard.tsx

# Confirms class-based dark mode mechanism CodeCallout's `dark:` classes rely on
@src/app/globals.css
@src/components/ThemeToggle.tsx

<interfaces>
<!-- Contracts that consumers in Phases 2–6 will rely on. Lock these exactly. -->

From src/lib/utils.ts (to be created):
```typescript
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]): string {
  return twMerge(clsx(inputs));
}
```

From src/components/CodeCallout.tsx (to be created):
```typescript
interface CodeCalloutProps {
  classes: string;
}

export default function CodeCallout({ classes }: Readonly<CodeCalloutProps>): JSX.Element;
```

Usage downstream:
```tsx
const CARD_CLASSES = "bg-white rounded-2xl p-6 shadow-sm border border-slate-200";
<div className={CARD_CLASSES}>...</div>
<CodeCallout classes={CARD_CLASSES} />
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create cn() utility at src/lib/utils.ts</name>
  <files>src/lib/utils.ts</files>
  <read_first>
    - package.json (verify clsx and tailwind-merge are installed — Plan 01 must have completed)
    - src/components/ThemeToggle.tsx (lines 3–4 — confirms `@/` path alias resolves to `src/`)
    - .planning/phases/01-shared-infrastructure/01-CONTEXT.md §cn() Utility (D-08, D-09)
    - .planning/phases/01-shared-infrastructure/01-PATTERNS.md §src/lib/utils.ts (concrete target structure)
  </read_first>
  <action>
    The directory `src/lib/` does not yet exist — create it as part of writing the file. Then write `src/lib/utils.ts` with EXACTLY this content (no additional helpers, no JSDoc beyond what is shown, no extra exports):

    ```typescript
    import { clsx, type ClassValue } from "clsx";
    import { twMerge } from "tailwind-merge";

    export function cn(...inputs: ClassValue[]): string {
      return twMerge(clsx(inputs));
    }
    ```

    Notes:
    - Named export `cn` — NOT default export (per PATTERNS.md: named exports for utility files).
    - Signature is `(...inputs: ClassValue[]): string` — variadic, ClassValue type imported from `clsx` (D-08).
    - Body is `twMerge(clsx(inputs))` — pass `inputs` (the array) directly to `clsx`; clsx flattens its argument internally.
    - Do NOT add a wrapping default export, a `cnDebug`, a memoization layer, or any other helper. D-08 forbids additional functionality.
    - Do NOT change the file path. D-09 locks `src/lib/utils.ts`.
  </action>
  <verify>
    <automated>test -f src/lib/utils.ts && grep -q "export function cn" src/lib/utils.ts && grep -q "from \"clsx\"" src/lib/utils.ts && grep -q "from \"tailwind-merge\"" src/lib/utils.ts && grep -q "twMerge(clsx(inputs))" src/lib/utils.ts && yarn build</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/utils.ts` exists
    - File contains the literal substring `export function cn(`
    - File contains the literal substring `...inputs: ClassValue[]`
    - File contains the literal substring `: string {` (return type annotation present)
    - File contains the literal substring `from "clsx"`
    - File contains the literal substring `from "tailwind-merge"`
    - File contains the literal substring `twMerge(clsx(inputs))`
    - File does NOT contain `export default` (named export only)
    - `yarn build` exits 0 (TypeScript resolves the imports — packages from Plan 01 are installed)
  </acceptance_criteria>
  <done>cn() is importable as `import { cn } from "@/lib/utils"`, type signature matches D-08, build passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create CodeCallout RSC at src/components/CodeCallout.tsx</name>
  <files>src/components/CodeCallout.tsx</files>
  <read_first>
    - src/components/NavCard.tsx (closest analog: RSC, typed props, Readonly<>, no "use client")
    - src/app/globals.css (confirm @custom-variant dark is present so dark: prefixes resolve)
    - .planning/phases/01-shared-infrastructure/01-CONTEXT.md §CodeCallout Appearance and §CodeCallout API (D-01 through D-07)
    - .planning/phases/01-shared-infrastructure/01-UI-SPEC.md §CodeCallout Component Specification (locked class string and rendered structure)
    - .planning/phases/01-shared-infrastructure/01-PATTERNS.md §src/components/CodeCallout.tsx (concrete target structure)
  </read_first>
  <action>
    Create `src/components/CodeCallout.tsx` as an RSC. Do NOT include `"use client"` — it is a pure display component with no state, refs, or browser APIs (D-06: fixed appearance).

    Write EXACTLY this file:

    ```tsx
    interface CodeCalloutProps {
      classes: string;
    }

    export default function CodeCallout({ classes }: Readonly<CodeCalloutProps>) {
      return (
        <p className="inline-block font-mono text-[13px] leading-relaxed bg-slate-100 text-slate-700 border border-slate-200 rounded-lg px-3 py-1 whitespace-pre-wrap break-all dark:bg-slate-800 dark:text-slate-300 dark:border-slate-700 3xl:text-base 3xl:px-4 3xl:py-2 3xl:rounded-xl">
          {classes}
        </p>
      );
    }
    ```

    Notes (each derived from a locked decision):
    - Single `<p>` element (UI-SPEC §Rendered HTML structure) — no wrapper div, callers manage layout context.
    - Props interface name: `CodeCalloutProps`. Single prop `classes: string` (D-05). Wrap in `Readonly<>` (PATTERNS.md §Readonly props pattern, NavCard.tsx line 10).
    - Default export named `CodeCallout` (matches NavCard.tsx export style).
    - No `className` / `size` / `variant` props (D-06 — fixed appearance).
    - No label prefix in the rendered text (D-04 — raw class string only).
    - Class string is one complete static literal — NO template literals, NO `cn()` calls, NO concatenation. The class string MUST be a single quoted string in source so Tailwind's purger picks up every class. (CLAUDE.md: never use dynamic class interpolation.)
    - Class string includes ALL of: `inline-block`, `font-mono`, `text-[13px]`, `leading-relaxed`, `bg-slate-100`, `text-slate-700`, `border`, `border-slate-200`, `rounded-lg`, `px-3`, `py-1`, `whitespace-pre-wrap`, `break-all`, `dark:bg-slate-800`, `dark:text-slate-300`, `dark:border-slate-700`, `3xl:text-base`, `3xl:px-4`, `3xl:py-2`, `3xl:rounded-xl`. (Locked by UI-SPEC §Class string.)
    - `whitespace-pre-wrap` + `break-all` together satisfy D-03 (never truncate, wrap to multiple lines).
    - `inline-block` keeps the chip from stretching full-width (PATTERNS.md note).
    - 3xl: escalations are mandatory (CLAUDE.md §Tailwind v4 specifics: "All demo components must include 3xl: escalations").
    - Render `{classes}` as the text child — exactly the prop value, nothing else.

    Do NOT:
    - Add `"use client"` (RSC default per UI-SPEC §RSC status).
    - Add a label like "classes:" or wrap in brackets `[ ... ]` in JSX (D-04 — raw string only; the bracket aesthetic from CONTEXT.md §Specifics is achieved by chip framing, not literal brackets).
    - Use `cn()` to compose the class string — the chip class string is fixed and static.
    - Add a `mt-3` / `mt-4` margin to the chip itself — PATTERNS.md included it as a suggestion but UI-SPEC §Class string locks the actual chip class string without `mt-*`. Containing slide pages control margin via their own layout.
    - Add accessibility attributes like `role="code"` — `<p>` element is correct per UI-SPEC.
  </action>
  <verify>
    <automated>test -f src/components/CodeCallout.tsx && grep -q "export default function CodeCallout(" src/components/CodeCallout.tsx && grep -q "interface CodeCalloutProps" src/components/CodeCallout.tsx && grep -q "classes: string" src/components/CodeCallout.tsx && grep -q "Readonly<CodeCalloutProps>" src/components/CodeCallout.tsx && grep -q "font-mono" src/components/CodeCallout.tsx && grep -q "bg-slate-100" src/components/CodeCallout.tsx && grep -q "dark:bg-slate-800" src/components/CodeCallout.tsx && grep -q "3xl:text-base" src/components/CodeCallout.tsx && grep -q "break-all" src/components/CodeCallout.tsx && grep -q "whitespace-pre-wrap" src/components/CodeCallout.tsx && ! grep -q '"use client"' src/components/CodeCallout.tsx && yarn build</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/CodeCallout.tsx` exists
    - File contains the literal substring `export default function CodeCallout(`
    - File contains the literal substring `interface CodeCalloutProps`
    - File contains the literal substring `classes: string`
    - File contains the literal substring `Readonly<CodeCalloutProps>`
    - File does NOT contain the literal substring `"use client"` (RSC, no directive)
    - Class string contains every required class (verified by individual greps): `font-mono`, `text-[13px]`, `bg-slate-100`, `text-slate-700`, `border-slate-200`, `rounded-lg`, `px-3`, `py-1`, `whitespace-pre-wrap`, `break-all`, `dark:bg-slate-800`, `dark:text-slate-300`, `dark:border-slate-700`, `3xl:text-base`, `3xl:px-4`, `3xl:py-2`, `3xl:rounded-xl`
    - File does NOT contain a backtick (`` ` ``) followed later by `${` on the same line (no template-literal class interpolation): `! grep -E '\`[^\`]*\$\{' src/components/CodeCallout.tsx`
    - File does NOT contain a `className` prop other than the literal class-attribute string on the `<p>` (no destructured `className` from props): `! grep -q "className: string" src/components/CodeCallout.tsx`
    - File does NOT contain `import.*cn.*from.*utils` (the chip is static — does not call cn())
    - Component has exactly one prop in its destructure (no `...rest`): `! grep -q "\.\.\." src/components/CodeCallout.tsx`
    - `yarn build` exits 0 (TypeScript compiles, no Tailwind class purge regressions)
    - `yarn lint` exits 0
  </acceptance_criteria>
  <done>CodeCallout is importable as default from `@/components/CodeCallout`, renders a single `<p>` chip with the locked class list, no client directive, build and lint pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | CodeCallout receives a `classes: string` prop that is rendered as text only — not as element className, not as innerHTML. No untrusted input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | CodeCallout `classes` text rendering | accept | Prop is rendered as React text child (`{classes}`), which React escapes by default. Even a malicious `classes` string cannot inject HTML. The component is used only with author-controlled compile-time string constants in slide pages — no user input source exists in this static presentation app. |
| T-01-05 | Tampering | src/lib/utils.ts cn() | accept | Pure function: `twMerge(clsx(inputs))`. No I/O, no globals, no module-scoped state. Inputs come from author-controlled component code in a static presentation app. |
</threat_model>

<verification>
- `cn()` importable: `import { cn } from "@/lib/utils"` resolves
- `cn("p-2", "p-4")` returns `"p-4"` (twMerge behavior — verified by build resolving twMerge)
- `CodeCallout` importable: `import CodeCallout from "@/components/CodeCallout"` resolves as default
- Rendering `<CodeCallout classes="bg-sky-500 rounded-lg px-4 py-2" />` produces a `<p>` with that exact text content
- `yarn build` and `yarn lint` both exit 0
</verification>

<success_criteria>
- INFRA-01 satisfied: CodeCallout RSC renders class strings in TV-legible chip styling, importable across slide pages
- INFRA-02 satisfied: cn() utility importable from `@/lib/utils`
- D-01 through D-09 all satisfied (chip appearance, API, single-prop, RSC, file path, signature)
- ROADMAP §Phase 1 success criteria 1, 2, 4 satisfied (callout adjacent to demo, cn() merging, packages clean)
</success_criteria>

<output>
After completion, create `.planning/phases/01-shared-infrastructure/01-03-SUMMARY.md`
</output>
