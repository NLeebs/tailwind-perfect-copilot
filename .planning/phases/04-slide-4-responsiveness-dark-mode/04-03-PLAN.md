---
phase: 04-slide-4-responsiveness-dark-mode
plan: 03
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
files_modified: []
autonomous: true
requirements:
  - S4-01
  - S4-02
  - S4-03
must_haves:
  truths:
    - "yarn build exits 0 — no Tailwind classes purged, no TypeScript errors, no lint errors"
    - "The responsiveness-dark-mode route is reachable from the home nav"
    - "Both new files (ResponsiveDemo.tsx and updated page.tsx) are committed and free of issues"
  artifacts:
    - path: "src/components/ResponsiveDemo.tsx"
      provides: "Client island — verified to build cleanly"
      contains: '"use client"'
    - path: "src/app/responsiveness-dark-mode/page.tsx"
      provides: "Full slide page — verified to build cleanly"
      contains: "DARK_CLASSES"
  key_links:
    - from: "home page slides array"
      to: "responsiveness-dark-mode route"
      via: "src/app/page.tsx slides entry"
      pattern: "responsiveness-dark-mode"
---

<objective>
Verify Phase 4 builds cleanly and the slide is reachable from the home nav. Run `yarn build` and `yarn lint`, confirm the home page `slides` array has a correct entry for the responsiveness-dark-mode route, and fix any issues.

Purpose: Production-quality gate — catches purged Tailwind classes, TypeScript errors, lint warnings, and missing nav registration before Phase 5 begins.
Output: Green build + confirmed home nav entry. No new files created.
</objective>

<execution_context>
@/Users/nathanlieberman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nathanlieberman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-slide-4-responsiveness-dark-mode/04-01-SUMMARY.md
@.planning/phases/04-slide-4-responsiveness-dark-mode/04-02-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. -->

From src/app/page.tsx — slides array pattern (existing entries for reference):
Each slide entry in the array looks like:
```tsx
{
  href: "/slug-here",
  number: "NN",
  title: "Slide Title",
  description: "One-line description.",
}
```
The responsiveness-dark-mode entry must have:
- href: "/responsiveness-dark-mode"
- number: "04"
- title that references responsiveness or dark mode
- A description that matches the talk's teaching goal
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and home nav audit</name>
  <files></files>

  <read_first>
    - src/app/page.tsx — check that the slides array has a "responsiveness-dark-mode" entry with correct title and description; fix if stale/missing
    - src/components/ResponsiveDemo.tsx — confirm file exists and looks correct before running build
    - src/app/responsiveness-dark-mode/page.tsx — confirm no "use client", both consts present
  </read_first>

  <action>
**Step 1 — Verify home nav entry.**

Read `src/app/page.tsx`. Find the `slides` array. Look for an entry with `href: "/responsiveness-dark-mode"`.

If the entry exists with a reasonable title and description, leave it as-is.

If the entry is missing, add it to the array in slide-number order:
```tsx
{
  href: "/responsiveness-dark-mode",
  number: "04",
  title: "Responsiveness & Dark Mode",
  description: "Breakpoint prefixes and dark: utilities that compose with one mental model.",
},
```

If the entry exists but has a placeholder title or description, update it to:
- title: `"Responsiveness & Dark Mode"`
- description: `"Breakpoint prefixes and dark: utilities that compose with one mental model."`

**Step 2 — Run build.**

Run `yarn build`. If it fails, read the error output, identify which file has the issue, fix it, and re-run. Common failure modes:
- Unescaped HTML entity in JSX (`'` → use `&apos;` or wrap in curly braces `{''}`)
- Import path typo (e.g., wrong case in filename)
- TypeScript error from missing prop or wrong type
- Tailwind class not being detected (verify no dynamic interpolation was accidentally introduced)

**Step 3 — Run lint.**

Run `yarn lint`. Fix any errors (warnings are acceptable if not from Phase 4 files). ESLint rules in this project include the `no-unescaped-entities` rule from `eslint-config-next`.

**Step 4 — Confirm route reachability.**

Verify that visiting `/responsiveness-dark-mode` would render the slide by confirming:
- `src/app/responsiveness-dark-mode/page.tsx` exists (it does after Plan 02)
- `src/app/responsiveness-dark-mode/` directory exists
- The file exports a default function

No manual browser check required — the build passing is sufficient verification that the route compiles correctly.
  </action>

  <verify>
    <automated>cd /Users/nathanlieberman/Documents/GWRepos/tailwind-perfect-copilot && yarn build 2>&1 | tail -10 && yarn lint 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `yarn build` exits 0 (no output containing "Error" or "Failed")
    - `yarn lint` exits 0 or exits with only warnings (no errors)
    - `src/app/page.tsx` contains `"responsiveness-dark-mode"` in the slides array
    - `src/app/page.tsx` contains `number: "04"` adjacent to the responsiveness-dark-mode entry (or equivalent — exact format depends on current page.tsx structure)
    - `src/app/responsiveness-dark-mode/page.tsx` exists (confirmed by build passing)
    - `src/components/ResponsiveDemo.tsx` exists (confirmed by build passing)
    - No TypeScript errors in build output referencing Phase 4 files
  </acceptance_criteria>

  <done>yarn build exits 0, yarn lint clean, home nav has a correct responsiveness-dark-mode entry. Phase 4 is production-ready.</done>
</task>

</tasks>

<verification>
1. `yarn build` exits 0
2. `yarn lint` exits 0
3. `grep -c 'responsiveness-dark-mode' src/app/page.tsx` returns >= 1
</verification>

<success_criteria>
- Clean production build with all Phase 4 files compiling without errors
- Home nav slides array correctly registers the responsiveness-dark-mode route
- No TypeScript, lint, or Tailwind purge issues introduced by Phase 4 work
</success_criteria>

<output>
After completion, create `.planning/phases/04-slide-4-responsiveness-dark-mode/04-03-SUMMARY.md`
</output>
