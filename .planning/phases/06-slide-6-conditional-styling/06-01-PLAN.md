---
phase: 06-slide-6-conditional-styling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ConditionalPanels.tsx
autonomous: true
requirements:
  - S6-01
must_haves:
  truths:
    - "ConditionalPanels renders three panels with overline labels CSS VARIANTS, GROUP VARIANTS, REACT STATE"
    - "Panel 1 card carries hover:bg-sky-50 and dark:hover:bg-slate-700 classes directly from HOVER_CLASSES const"
    - "Panel 2 card parent has group class; avatar child carries GROUP_AVATAR_CLASSES; text child carries GROUP_TEXT_CLASSES"
    - "Panel 3 renders a Toggle button; clicking it toggles isActive and switches card classes via PANEL3_CLASSES lookup map"
    - "CodeCallout below each panel receives the single-source const — no literal strings duplicated on the element and the callout"
    - "File uses use client directive; page.tsx does NOT import useState — island is a leaf node"
    - "No dynamic class interpolation — all state-driven class strings are complete static values in PANEL3_CLASSES"
  artifacts:
    - path: "src/components/ConditionalPanels.tsx"
      provides: "Three-panel Section 1 client island"
      contains: "use client"
    - path: "src/components/ConditionalPanels.tsx"
      provides: "Single-source const declarations"
      contains: "HOVER_CLASSES"
    - path: "src/components/ConditionalPanels.tsx"
      provides: "Lookup-table map for React state toggle"
      contains: "PANEL3_CLASSES"
  key_links:
    - from: "ConditionalPanels.tsx Panel 1 card className"
      to: "CodeCallout classes prop"
      via: "HOVER_CLASSES const"
      pattern: "HOVER_CLASSES"
    - from: "ConditionalPanels.tsx Panel 3 card className"
      to: "PANEL3_CLASSES lookup"
      via: "cn() with isActive ternary key"
      pattern: "PANEL3_CLASSES\\[isActive"
---

<objective>
Create `src/components/ConditionalPanels.tsx` — the "use client" island that renders all of Section 1 (the three-panel comparison demonstrating CSS hover variants, group-hover variants, and React state + cn()).

Purpose: Section 1 of the conditional-styling slide teaches three distinct mechanisms for class-driven appearance changes in a side-by-side format. Having all three panels in one island keeps the DOM structure clean and the comparison visually coherent (the React state panel requires useState, which pulls the entire section into client rendering).

Output: `src/components/ConditionalPanels.tsx` — a complete, production-ready client island. No other files are touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-slide-6-conditional-styling/06-CONTEXT.md
@.planning/phases/06-slide-6-conditional-styling/06-RESEARCH.md
@.planning/phases/06-slide-6-conditional-styling/06-UI-SPEC.md

<interfaces>
<!-- Key contracts the executor must replicate exactly. -->

From src/components/CodeCallout.tsx:
```typescript
interface CodeCalloutProps {
  classes: string;
}
export default function CodeCallout({ classes }: Readonly<CodeCalloutProps>): JSX.Element
// Renders a mono badge. Multi-line supported via whitespace-pre-wrap.
// Already handles 3xl: escalation internally — do NOT add 3xl overrides to CodeCallout.
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
// Import: import { cn } from "@/lib/utils";
// Use for Panel 3 card className only — cn("base-classes", PANEL3_CLASSES[key])
```

Established overline style (from CardBuilder.tsx, customizing-tailwind/page.tsx):
```
text-xs font-semibold tracking-widest uppercase text-cyan-600 dark:text-cyan-400 3xl:text-base
```

Established button style for toggle buttons (from SlideLayout context):
```
px-4 py-2 3xl:px-6 3xl:py-3 rounded-lg bg-slate-100 dark:bg-slate-800
text-sm 3xl:text-base font-semibold text-slate-700 dark:text-slate-300
hover:bg-slate-200 dark:hover:bg-slate-700 transition-colors
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ConditionalPanels.tsx client island</name>
  <files>src/components/ConditionalPanels.tsx</files>
  <read_first>
    - src/components/CardBuilder.tsx — reference for "use client", useState, cn(), lookup-table map pattern, CodeCallout usage, overline style
    - src/components/ResponsiveDemo.tsx — reference for client island structure, tab-state toggle, button styling
    - src/components/CodeCallout.tsx — confirm prop interface (classes: string) before writing
    - src/lib/utils.ts — confirm cn() import path
    - .planning/phases/06-slide-6-conditional-styling/06-UI-SPEC.md — §Component Inventory, §Single-Source Constants, §Lookup-Table Maps, §3xl: Escalation Table, §Copywriting Contract
  </read_first>
  <behavior>
    - Renders three-column grid with three self-contained panels
    - Panel 1 CodeCallout text equals exactly: "hover:bg-sky-50 dark:hover:bg-slate-700"
    - Panel 2 CodeCallout text equals exactly: "group-hover:scale-105 group-hover:shadow-lg"
    - Panel 3 CodeCallout text equals exactly: "cn('rounded-xl p-6 transition-colors',\n  isActive && 'bg-blue-500 text-white')"
    - Clicking Toggle button flips isActive boolean
    - When isActive=false, Panel 3 card has classes from PANEL3_CLASSES.inactive
    - When isActive=true, Panel 3 card has classes from PANEL3_CLASSES.active (bg-blue-500 text-white shadow-md)
    - No dynamic class interpolation anywhere
  </behavior>
  <action>
Create `src/components/ConditionalPanels.tsx` with the following exact implementation:

```tsx
"use client";
import { useState } from "react";
import CodeCallout from "@/components/CodeCallout";
import { cn } from "@/lib/utils";

// ── Single-source consts ── same value used by element className AND CodeCallout.classes ──

const HOVER_CLASSES = "hover:bg-sky-50 dark:hover:bg-slate-700";

// GROUP_CALLOUT is what the CodeCallout displays for Panel 2
const GROUP_CALLOUT = "group-hover:scale-105 group-hover:shadow-lg";
// The actual classes applied to child elements inside the group parent
const GROUP_AVATAR_CLASSES =
  "group-hover:scale-105 group-hover:bg-sky-200 dark:group-hover:bg-sky-900";
const GROUP_TEXT_CLASSES =
  "group-hover:text-sky-700 dark:group-hover:text-sky-300";

// STATE_CALLOUT shows the cn() call as a readable string for the audience
const STATE_CALLOUT =
  "cn('rounded-xl p-6 transition-colors',\n  isActive && 'bg-blue-500 text-white')";

// ── Lookup-table map — complete static strings, no dynamic interpolation ──
const PANEL3_CLASSES: Record<"active" | "inactive", string> = {
  active: "bg-blue-500 text-white shadow-md",
  inactive:
    "bg-white dark:bg-slate-900 text-slate-700 dark:text-slate-300 border border-slate-200 dark:border-slate-700",
};

const OVERLINE =
  "text-xs font-semibold tracking-widest uppercase text-cyan-600 dark:text-cyan-400 3xl:text-base";
const SUBLABEL =
  "text-xs text-slate-500 dark:text-slate-400 3xl:text-sm mt-2";

export default function ConditionalPanels() {
  const [isActive, setIsActive] = useState(false);

  return (
    <div className="grid grid-cols-3 gap-6 3xl:gap-8">
      {/* ── Panel 1: CSS VARIANTS ── */}
      <div className="flex flex-col">
        <p className={`${OVERLINE} mb-4 3xl:mb-6`}>CSS VARIANTS</p>
        <div
          className={cn(
            "rounded-xl p-6 3xl:p-8 bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-700 transition-colors duration-200 cursor-pointer",
            HOVER_CLASSES
          )}
        >
          <p className="font-semibold text-slate-900 dark:text-white text-sm 3xl:text-xl">
            Hover me
          </p>
          <p className="text-sm text-slate-600 dark:text-slate-400 mt-2 3xl:text-xl 3xl:mt-3">
            Pure CSS variant — no JS
          </p>
        </div>
        <div className="mt-3">
          <CodeCallout classes={HOVER_CLASSES} />
        </div>
        <p className={SUBLABEL}>No JavaScript — pure CSS <code>:hover</code></p>
      </div>

      {/* ── Panel 2: GROUP VARIANTS ── */}
      <div className="flex flex-col">
        <p className={`${OVERLINE} mb-4 3xl:mb-6`}>GROUP VARIANTS</p>
        <div
          className="group rounded-xl p-6 3xl:p-8 bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-700 cursor-pointer transition-colors duration-200"
        >
          {/* Avatar — scales and changes color on parent hover */}
          <div
            className={cn(
              "size-10 3xl:size-14 rounded-full bg-slate-200 dark:bg-slate-700 mb-4 transition-all duration-200",
              GROUP_AVATAR_CLASSES
            )}
          />
          {/* Text — shifts color on parent hover */}
          <p
            className={cn(
              "text-sm 3xl:text-xl text-slate-600 dark:text-slate-400 transition-colors duration-200",
              GROUP_TEXT_CLASSES
            )}
          >
            Hover the card
          </p>
        </div>
        <div className="mt-3">
          <CodeCallout classes={GROUP_CALLOUT} />
        </div>
        <p className={SUBLABEL}>
          Parent <code>group</code> drives all children simultaneously
        </p>
      </div>

      {/* ── Panel 3: REACT STATE ── */}
      <div className="flex flex-col">
        <p className={`${OVERLINE} mb-4 3xl:mb-6`}>REACT STATE</p>
        <div
          className={cn(
            "rounded-xl p-6 3xl:p-8 transition-colors duration-200",
            PANEL3_CLASSES[isActive ? "active" : "inactive"]
          )}
        >
          <p className="font-semibold text-sm 3xl:text-xl">
            Status: {isActive ? "Active" : "Inactive"}
          </p>
        </div>
        <button
          type="button"
          onClick={() => setIsActive((v) => !v)}
          className="mt-4 px-4 py-2 3xl:px-6 3xl:py-3 rounded-lg bg-slate-100 dark:bg-slate-800 text-sm 3xl:text-base font-semibold text-slate-700 dark:text-slate-300 hover:bg-slate-200 dark:hover:bg-slate-700 transition-colors self-start"
        >
          Toggle
        </button>
        <div className="mt-3">
          <CodeCallout classes={STATE_CALLOUT} />
        </div>
        <p className={SUBLABEL}>Explicit React state — full programmatic control</p>
      </div>
    </div>
  );
}
```

Key constraints to verify during implementation:
- File starts with `"use client"` directive (line 1)
- HOVER_CLASSES used as both a className spread AND the CodeCallout classes prop — same const reference, no copy-pasting the string
- GROUP_AVATAR_CLASSES and GROUP_TEXT_CLASSES applied to child elements inside the parent that carries the `group` class
- GROUP_CALLOUT (not GROUP_AVATAR_CLASSES) is what goes into the Panel 2 CodeCallout — it shows the "teaching" string
- PANEL3_CLASSES is a Record with complete static string values — no string concatenation or template literals
- cn() used for PANEL3_CLASSES composition: `cn("base-classes", PANEL3_CLASSES[isActive ? "active" : "inactive"])`
- 3xl: escalation on every text size and spacing value per the escalation table
- No "use client" added to page.tsx — this island is the only client boundary
  </action>
  <verify>
    <automated>cd /Users/nathanlieberman/Documents/GWRepos/tailwind-perfect-copilot && grep -c '"use client"' src/components/ConditionalPanels.tsx && grep -c 'HOVER_CLASSES\|GROUP_CALLOUT\|PANEL3_CLASSES' src/components/ConditionalPanels.tsx && yarn test --run 2>&1 | tail -5</automated>
  </verify>
  <done>
    - src/components/ConditionalPanels.tsx exists
    - File begins with "use client"
    - HOVER_CLASSES, GROUP_CALLOUT, GROUP_AVATAR_CLASSES, GROUP_TEXT_CLASSES, STATE_CALLOUT, PANEL3_CLASSES all declared as module-scope consts
    - PANEL3_CLASSES is a Record with "active" and "inactive" keys holding complete static strings
    - Three panels rendered in grid grid-cols-3 gap-6 3xl:gap-8
    - Each panel has overline label, demo card, CodeCallout, and sub-label
    - No dynamic class interpolation (grep for backtick-dollar patterns finds zero)
    - yarn test passes (existing 61 tests green — ConditionalPanels.test.tsx may not exist yet and that is expected at Wave 1)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static island | No user data crosses boundaries — all state is ephemeral boolean toggle, no persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | ConditionalPanels.tsx className values | accept | Demo-only component, no user data, no server interaction — class string manipulation has no security impact |
| T-06-02 | Information Disclosure | None | accept | Component renders no PII, secrets, or sensitive state |
</threat_model>

<verification>
After task completes:

```bash
cd /Users/nathanlieberman/Documents/GWRepos/tailwind-perfect-copilot

# 1. File exists with "use client"
grep -c '"use client"' src/components/ConditionalPanels.tsx
# Expected: 1

# 2. All single-source consts present
grep -c 'HOVER_CLASSES\|GROUP_CALLOUT\|GROUP_AVATAR_CLASSES\|GROUP_TEXT_CLASSES\|STATE_CALLOUT\|PANEL3_CLASSES' src/components/ConditionalPanels.tsx
# Expected: 6+ (const declarations + usages)

# 3. Lookup-table map uses complete static strings (no template literals)
grep -v '^//' src/components/ConditionalPanels.tsx | grep -c '`.*\${'
# Expected: 0

# 4. No "use client" in page.tsx (this file not touched)
test ! -f src/app/conditional-styling/page.tsx || grep -c '"use client"' src/app/conditional-styling/page.tsx || echo "0 — correct"

# 5. Existing tests still green
yarn test --run 2>&1 | tail -5
```
</verification>

<success_criteria>
- src/components/ConditionalPanels.tsx created with "use client" on line 1
- All 6 single-source consts declared at module scope (HOVER_CLASSES, GROUP_CALLOUT, GROUP_AVATAR_CLASSES, GROUP_TEXT_CLASSES, STATE_CALLOUT, PANEL3_CLASSES)
- PANEL3_CLASSES is a Record<"active" | "inactive", string> with complete static values
- Three panels in grid grid-cols-3 gap-6 3xl:gap-8 with overline / demo card / CodeCallout / sub-label stacks
- cn() used for PANEL3_CLASSES application
- No dynamic class interpolation
- yarn test exits 0 (existing tests green)
</success_criteria>

<output>
After completion, create `.planning/phases/06-slide-6-conditional-styling/06-01-SUMMARY.md`
</output>
