---
phase: 06-slide-6-conditional-styling
plan: 04
type: execute
wave: 3
depends_on:
  - "06-03"
files_modified:
  - src/test/components/ConditionalPanels.test.tsx
  - src/test/components/DataActiveDemo.test.tsx
  - src/test/app/slide-pages.test.tsx
autonomous: true
requirements:
  - S6-01
  - S6-02
  - S6-03
  - S6-04
must_haves:
  truths:
    - "ConditionalPanels.test.tsx covers panel overline labels, state toggle, and CodeCallout content for S6-01"
    - "DataActiveDemo.test.tsx covers initial render, Toggle Active button, and data-active attribute toggling for S6-04"
    - "slide-pages.test.tsx no longer lists 06 in stubCases"
    - "slide-pages.test.tsx has a describe(Conditional Styling content) block with S6-01 through S6-04 assertions"
    - "yarn test exits 0 with all tests green including the new ones"
  artifacts:
    - path: "src/test/components/ConditionalPanels.test.tsx"
      provides: "Unit tests for S6-01 ConditionalPanels island"
      min_lines: 40
    - path: "src/test/components/DataActiveDemo.test.tsx"
      provides: "Unit tests for S6-04 DataActiveDemo island"
      min_lines: 30
    - path: "src/test/app/slide-pages.test.tsx"
      provides: "Updated stub list + Conditional Styling content assertions"
      contains: "Conditional Styling content"
  key_links:
    - from: "slide-pages.test.tsx stubCases"
      to: "06 removed from filter array"
      via: "allCases.filter where 06 is no longer included"
      pattern: "stubCases"
    - from: "ConditionalPanels.test.tsx fireEvent.click"
      to: "Toggle button state change"
      via: "fireEvent.click + assertion on card class change"
      pattern: "Toggle"
    - from: "DataActiveDemo.test.tsx fireEvent.click"
      to: "data-active attribute presence"
      via: "fireEvent.click + getAttribute('data-active')"
      pattern: "data-active"
---

<objective>
Write all Phase 6 tests: create `ConditionalPanels.test.tsx` and `DataActiveDemo.test.tsx`, then update `slide-pages.test.tsx` to remove "06" from stubCases and add a `describe("Conditional Styling content")` block with assertions covering S6-01 through S6-04. Run yarn test as the final gate.

Purpose: Tests verify the islands behave correctly and prevent regressions. The slide-pages.test.tsx update is mandatory — without removing "06" from stubCases, the existing test will FAIL because "Content coming soon" is no longer in the DOM after Plan 03 replaced the stub.

Output: Three test files written/updated; yarn test exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-slide-6-conditional-styling/06-RESEARCH.md
@.planning/phases/06-slide-6-conditional-styling/06-VALIDATION.md

<interfaces>
<!-- Key test infrastructure contracts. -->

From src/test/components/CardBuilder.test.tsx (reference test pattern):
```typescript
import { render, screen, fireEvent } from "@testing-library/react";
import { describe, it, expect } from "vitest";
import ComponentName from "@/components/ComponentName";

describe("ComponentName", () => {
  it("renders without error — contains expected text", () => {
    render(<ComponentName />);
    expect(screen.getByText("expected text")).toBeInTheDocument();
  });

  it("clicking button changes state", () => {
    render(<ComponentName />);
    const btn = screen.getByRole("button", { name: /button label/i });
    fireEvent.click(btn);
    // assert new state
  });
});
```

Hover behavior note (CRITICAL for test design):
  jsdom does NOT fire :hover pseudo-class. Tests MUST NOT attempt to verify hover state
  changes. Test structural rendering only — class presence, text content, button presence.

data-active attribute testing:
  React sets data-active="" when value is "" and omits it when undefined.
  Test: after clicking "Toggle Active", the card div should have getAttribute('data-active') === ""
  Before click: getAttribute('data-active') === null (attribute absent)

From src/test/app/slide-pages.test.tsx (current content — must update):
  - Line 22-24: stubCases filters allCases where number is in ["06"]
  - Must change to: stubCases filters allCases where number is in [] (empty — "06" removed)
  - Must add: describe("Conditional Styling content") block after the existing "Customizing Tailwind content" block

From src/components/ConditionalPanels.tsx (Wave 1 output — read actual file):
  - Overline texts: "CSS VARIANTS", "GROUP VARIANTS", "REACT STATE"
  - Toggle button label: "Toggle"
  - Initial card body text: "Status: Inactive"
  - After click: "Status: Active"

From src/components/DataActiveDemo.tsx (Wave 1 output — read actual file):
  - Toggle button label: "Toggle Active"
  - Initial status text: "Status: Inactive"
  - After click: "Status: Active"
  - Card div carries data-active attribute (absent initially, present after click)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ConditionalPanels.test.tsx and DataActiveDemo.test.tsx</name>
  <files>
    src/test/components/ConditionalPanels.test.tsx
    src/test/components/DataActiveDemo.test.tsx
  </files>
  <read_first>
    - src/test/components/CardBuilder.test.tsx — reference for test file structure, fireEvent usage, assertion patterns
    - src/components/ConditionalPanels.tsx — read actual implemented file to get exact text content, button labels, and overline strings before writing assertions
    - src/components/DataActiveDemo.tsx — read actual implemented file to get exact text content, button label, and attribute patterns before writing assertions
    - .planning/phases/06-slide-6-conditional-styling/06-RESEARCH.md — §Common Pitfalls §Pitfall 6 (hover: variant not triggering in jsdom — do NOT test hover state)
    - .planning/phases/06-slide-6-conditional-styling/06-VALIDATION.md — §Per-Task Verification Map (test coverage requirements for S6-01, S6-04)
  </read_first>
  <action>
Create `src/test/components/ConditionalPanels.test.tsx`:

```tsx
import { render, screen, fireEvent } from "@testing-library/react";
import { describe, it, expect } from "vitest";
import ConditionalPanels from "@/components/ConditionalPanels";

describe("ConditionalPanels", () => {
  it("renders all three panel overline labels", () => {
    render(<ConditionalPanels />);
    expect(screen.getByText("CSS VARIANTS")).toBeInTheDocument();
    expect(screen.getByText("GROUP VARIANTS")).toBeInTheDocument();
    expect(screen.getByText("REACT STATE")).toBeInTheDocument();
  });

  it("renders Panel 1 with hover class callout text", () => {
    render(<ConditionalPanels />);
    // CodeCallout for Panel 1 shows HOVER_CLASSES value
    expect(
      screen.getByText("hover:bg-sky-50 dark:hover:bg-slate-700")
    ).toBeInTheDocument();
  });

  it("renders Panel 2 with group-hover class callout text", () => {
    render(<ConditionalPanels />);
    // CodeCallout for Panel 2 shows GROUP_CALLOUT value
    expect(
      screen.getByText("group-hover:scale-105 group-hover:shadow-lg")
    ).toBeInTheDocument();
  });

  it("renders Panel 3 in inactive state initially — shows Status: Inactive", () => {
    render(<ConditionalPanels />);
    expect(screen.getByText("Status: Inactive")).toBeInTheDocument();
  });

  it("renders Toggle button in Panel 3", () => {
    render(<ConditionalPanels />);
    expect(screen.getByRole("button", { name: /toggle/i })).toBeInTheDocument();
  });

  it("clicking Toggle button switches Panel 3 to active state — shows Status: Active", () => {
    render(<ConditionalPanels />);
    const toggleBtn = screen.getByRole("button", { name: /toggle/i });
    fireEvent.click(toggleBtn);
    expect(screen.getByText("Status: Active")).toBeInTheDocument();
  });

  it("clicking Toggle button twice returns to inactive state — shows Status: Inactive", () => {
    render(<ConditionalPanels />);
    const toggleBtn = screen.getByRole("button", { name: /toggle/i });
    fireEvent.click(toggleBtn);
    fireEvent.click(toggleBtn);
    expect(screen.getByText("Status: Inactive")).toBeInTheDocument();
  });

  it("Panel 3 card has active background class after clicking Toggle", () => {
    render(<ConditionalPanels />);
    const toggleBtn = screen.getByRole("button", { name: /toggle/i });
    // Find the Panel 3 card — it contains the Status text
    const statusText = screen.getByText("Status: Inactive");
    const card = statusText.closest("div");
    expect(card).not.toHaveClass("bg-blue-500");
    fireEvent.click(toggleBtn);
    const activeStatusText = screen.getByText("Status: Active");
    const activeCard = activeStatusText.closest("div");
    expect(activeCard).toHaveClass("bg-blue-500");
  });
});
```

Create `src/test/components/DataActiveDemo.test.tsx`:

```tsx
import { render, screen, fireEvent } from "@testing-library/react";
import { describe, it, expect } from "vitest";
import DataActiveDemo from "@/components/DataActiveDemo";

describe("DataActiveDemo", () => {
  it("renders without error — shows initial inactive status", () => {
    render(<DataActiveDemo />);
    expect(screen.getByText("Status: Inactive")).toBeInTheDocument();
  });

  it("renders the Toggle Active button", () => {
    render(<DataActiveDemo />);
    expect(
      screen.getByRole("button", { name: /toggle active/i })
    ).toBeInTheDocument();
  });

  it("renders the data-active CodeCallout", () => {
    render(<DataActiveDemo />);
    // DATA_CALLOUT has \n — rendered as two lines via whitespace-pre-wrap
    // Both class names should appear in the rendered callout text
    expect(
      screen.getByText(/data-active:bg-purple-600/)
    ).toBeInTheDocument();
  });

  it("renders headless UI explanation text", () => {
    render(<DataActiveDemo />);
    expect(
      screen.getByText(/Headless UI libraries/i)
    ).toBeInTheDocument();
  });

  it("clicking Toggle Active changes status to Active", () => {
    render(<DataActiveDemo />);
    const btn = screen.getByRole("button", { name: /toggle active/i });
    fireEvent.click(btn);
    expect(screen.getByText("Status: Active")).toBeInTheDocument();
  });

  it("clicking Toggle Active adds data-active attribute to the card", () => {
    render(<DataActiveDemo />);
    const statusText = screen.getByText("Status: Inactive");
    // The card is the div wrapping the status text
    const card = statusText.closest("div[class*='rounded-xl']");
    expect(card).not.toHaveAttribute("data-active");
    const btn = screen.getByRole("button", { name: /toggle active/i });
    fireEvent.click(btn);
    const activeCard = screen.getByText("Status: Active").closest("div[class*='rounded-xl']");
    expect(activeCard).toHaveAttribute("data-active");
  });

  it("clicking Toggle Active twice removes data-active attribute", () => {
    render(<DataActiveDemo />);
    const btn = screen.getByRole("button", { name: /toggle active/i });
    fireEvent.click(btn);
    fireEvent.click(btn);
    expect(screen.getByText("Status: Inactive")).toBeInTheDocument();
    const card = screen.getByText("Status: Inactive").closest("div[class*='rounded-xl']");
    expect(card).not.toHaveAttribute("data-active");
  });
});
```

Note on test selectors: If `closest("div[class*='rounded-xl']")` is too broad (returns wrong ancestor), use `closest('[data-active], [class*="rounded-xl p-6"]')` or restructure the query to find the card via its container. Read the actual implemented DataActiveDemo.tsx before writing the final selector.

Run `yarn test --run` after creating both files to verify all new tests pass. Fix any selector issues by reading the actual rendered output.
  </action>
  <verify>
    <automated>cd /Users/nathanlieberman/Documents/GWRepos/tailwind-perfect-copilot && yarn test --run 2>&1 | tail -15</automated>
  </verify>
  <done>
    - src/test/components/ConditionalPanels.test.tsx created with 8 test cases
    - src/test/components/DataActiveDemo.test.tsx created with 7 test cases
    - All new tests pass (yarn test exits 0)
    - No existing tests regressed
  </done>
</task>

<task type="auto">
  <name>Task 2: Update slide-pages.test.tsx — remove 06 from stubCases, add Conditional Styling content block</name>
  <files>src/test/app/slide-pages.test.tsx</files>
  <read_first>
    - src/test/app/slide-pages.test.tsx — READ THE CURRENT FILE FIRST to see exact stubCases filter line and the describe("Customizing Tailwind content") block pattern to mirror
    - src/app/conditional-styling/page.tsx — read implemented page to confirm content strings for assertions
    - src/components/ConditionalPanels.tsx — confirm rendered text content (overline labels, button label)
    - src/components/DataActiveDemo.tsx — confirm "Toggle Active" button label and headless UI text
  </read_first>
  <action>
Update `src/test/app/slide-pages.test.tsx` with two changes:

**Change 1 — Remove "06" from stubCases filter:**

Find the line:
```typescript
const stubCases = allCases.filter(({ number }) =>
  ["06"].includes(number)
);
```

Replace with:
```typescript
const stubCases = allCases.filter(({ number }) =>
  ([] as string[]).includes(number)
);
```

This makes stubCases an empty array — the "Stub pages (content coming soon)" describe block will have no test cases to iterate over, which is correct because all slides are now implemented.

**Change 2 — Add describe("Conditional Styling content") block:**

Append after the existing `describe("Customizing Tailwind content")` block:

```typescript
describe("Conditional Styling content", () => {
  it("renders all three panel overline labels (S6-01)", () => {
    render(<ConditionalStyling />);
    expect(screen.getByText("CSS VARIANTS")).toBeInTheDocument();
    expect(screen.getByText("GROUP VARIANTS")).toBeInTheDocument();
    expect(screen.getByText("REACT STATE")).toBeInTheDocument();
  });

  it("renders the Panel 3 Toggle button (S6-01)", () => {
    render(<ConditionalStyling />);
    expect(screen.getByRole("button", { name: /^toggle$/i })).toBeInTheDocument();
  });

  it("renders the Toggle Active button in DataActiveDemo (S6-04)", () => {
    render(<ConditionalStyling />);
    expect(screen.getByRole("button", { name: /toggle active/i })).toBeInTheDocument();
  });

  it("renders email input with type email and placeholder (S6-03)", () => {
    render(<ConditionalStyling />);
    const input = screen.getByRole("textbox", { name: /email address/i });
    expect(input).toHaveAttribute("type", "email");
    expect(input).toHaveAttribute("placeholder", "you@example.com");
  });

  it("clicking Toggle Active adds data-active attribute to the data card (S6-04)", () => {
    render(<ConditionalStyling />);
    const btn = screen.getByRole("button", { name: /toggle active/i });
    fireEvent.click(btn);
    expect(screen.getByText("Status: Active")).toBeInTheDocument();
  });
});
```

The full updated file should look like (add fireEvent import to existing import line if not present):

```typescript
import { render, screen, fireEvent } from "@testing-library/react";
import { describe, it, expect } from "vitest";
// ... existing imports ...
```

Verify `fireEvent` is imported from "@testing-library/react" in the existing import line. If it is not, add it.

After editing, run `yarn test --run` to confirm all tests pass — including the regression test that "06" slide renders slide number "06" and heading "Conditional Styling" (these are in allCases, not stubCases, so they still run).
  </action>
  <verify>
    <automated>cd /Users/nathanlieberman/Documents/GWRepos/tailwind-perfect-copilot && yarn test --run 2>&1 | tail -20</automated>
  </verify>
  <done>
    - slide-pages.test.tsx stubCases no longer includes "06"
    - describe("Conditional Styling content") block added with 5 test cases (S6-01 through S6-04)
    - fireEvent imported from "@testing-library/react" in slide-pages.test.tsx
    - yarn test exits 0 — all tests green
    - Test count increased from 61 baseline (61 + new ConditionalPanels tests + new DataActiveDemo tests + new Conditional Styling content tests)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test environment | Tests run in jsdom — no network, no server, no user data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Tampering | Test assertions | accept | Tests are dev tooling — no production security surface |
</threat_model>

<verification>
Final phase gate — run after both tasks complete:

```bash
cd /Users/nathanlieberman/Documents/GWRepos/tailwind-perfect-copilot

# 1. All tests pass
yarn test --run 2>&1 | tail -5
# Expected: exit 0, all tests green

# 2. stubCases no longer lists "06"
grep 'stubCases' src/test/app/slide-pages.test.tsx
# Expected: filter array is empty [] — "06" absent

# 3. New describe block present
grep -c 'Conditional Styling content' src/test/app/slide-pages.test.tsx
# Expected: 1

# 4. Both new test files exist
ls src/test/components/ConditionalPanels.test.tsx src/test/components/DataActiveDemo.test.tsx
# Expected: both files listed

# 5. Test count increased from 61 baseline
yarn test --run 2>&1 | grep -E '[0-9]+ passed'
# Expected: number > 61
```
</verification>

<success_criteria>
- src/test/components/ConditionalPanels.test.tsx created — tests panel overlines, state toggle, CodeCallout content
- src/test/components/DataActiveDemo.test.tsx created — tests render, toggle button, data-active attribute
- src/test/app/slide-pages.test.tsx updated: "06" removed from stubCases, describe("Conditional Styling content") added
- yarn test exits 0 with all tests green
- Test count > 61 (prior baseline)
</success_criteria>

<output>
After completion, create `.planning/phases/06-slide-6-conditional-styling/06-04-SUMMARY.md`
</output>
