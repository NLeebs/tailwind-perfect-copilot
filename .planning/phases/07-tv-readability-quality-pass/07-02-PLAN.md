---
phase: 07-tv-readability-quality-pass
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
autonomous: true
requirements:
  - QA-02

must_haves:
  truths:
    - "animate-reveal-up is the only entrance animation in the codebase and is already gated by the globals.css reduced-motion block"
    - "No other animate-* classes exist in the codebase that are unaccounted for"
    - "The globals.css @media (prefers-reduced-motion: reduce) block is the sole reduced-motion mechanism"
    - "No motion-reduce: Tailwind prefix is added to interactive transitions (hover, color fades, scale)"
    - "CssTimeline IntersectionObserver logic is unchanged"
  artifacts:
    - path: "src/app/globals.css"
      provides: "@media (prefers-reduced-motion: reduce) block with keyframe override for every entrance animation"
      contains: "@media (prefers-reduced-motion: reduce)"
  key_links:
    - from: "src/components/CssTimeline.tsx"
      to: "src/app/globals.css"
      via: "animate-reveal-up class triggers @keyframes reveal-up; reduced-motion block overrides it to a no-op"
      pattern: "animate-reveal-up"
---

<objective>
Reduced-motion audit across the full codebase to confirm all entrance animations respect prefers-reduced-motion.

Purpose: Users with vestibular disorders or motion sensitivity rely on prefers-reduced-motion to suppress animations. This plan confirms that the existing globals.css pattern covers all entrance animations and that no new entrance animations were added in Phases 2–6 without coverage.

Output: Confirmed (or patched) globals.css so that every animate-* entrance animation class in the codebase has a keyframe override in the @media (prefers-reduced-motion: reduce) block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Existing reduced-motion mechanism in globals.css (lines 34-39) -->
<!-- This is the ONLY mechanism — do NOT add motion-reduce: Tailwind prefixes to components -->

@media (prefers-reduced-motion: reduce) {
  @keyframes reveal-up {
    from { opacity: 1; transform: translateY(0); }
    to   { opacity: 1; transform: translateY(0); }
  }
}

<!-- Pattern: keyframe override makes the animation a no-op (starts and ends at final state) -->
<!-- This approach works with CssTimeline's IntersectionObserver: the element appears -->
<!-- immediately at opacity:1/translateY(0) instead of animating. No JS logic change needed. -->

<!-- DECISIONS FROM CONTEXT.md — NON-NEGOTIABLE -->
<!-- D-01: Gate entrance/exit animations only. Interactive transitions (hover, color fades) are fine as-is. -->
<!-- D-02: Do NOT add motion-reduce:transition-none to transition-colors, transition-all, transition-transform. -->
<!-- D-03: Do NOT add motion-reduce:scale-100 to group-hover:scale-105 in ConditionalPanels. -->
<!-- D-04: Do NOT modify CssTimeline IntersectionObserver logic. -->
<!-- D-05: Audit scope — confirm animate-reveal-up is the ONLY entrance animation; add any others found. -->

<!-- PRE-CONFIRMED FINDING (from pre-read grep in planning) -->
<!-- grep -rn "animate-" src/ returns ONLY CssTimeline.tsx -->
<!-- No other animate-* classes exist anywhere in the codebase -->
<!-- Therefore the globals.css block is already complete and sufficient -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all animate-* usages and confirm globals.css reduced-motion coverage</name>
  <files>
    src/app/globals.css
  </files>
  <read_first>
    - src/app/globals.css (read the full file — confirm the @media (prefers-reduced-motion: reduce) block at lines 34-39 and the @keyframes reveal-up at lines 29-32)
    - src/components/CssTimeline.tsx (confirm animate-reveal-up is the only animate-* usage and that IntersectionObserver logic is not changed)
  </read_first>
  <action>
    Step 1 — Full codebase scan for animate-* classes:

    Run this grep to find every animate-* class usage across all TypeScript/TSX files:
    ```
    grep -rn "animate-" src/
    ```

    Expected result: only CssTimeline.tsx appears (using `animate-reveal-up`). If no other files appear, the audit is complete — the existing globals.css block already covers the only entrance animation.

    Step 2 — If any NEW animate-* classes are found (classes other than `animate-reveal-up`):

    For each new `animate-<name>` found:
    a. Confirm it is an entrance/exit animation (not a Tailwind built-in like `animate-spin`, `animate-pulse`, `animate-bounce` — those are utility spinners, not entrance animations and do NOT need reduced-motion gating)
    b. If it IS an entrance animation driven by a custom `@keyframes <name>` in globals.css, add a keyframe override block inside the existing `@media (prefers-reduced-motion: reduce)` block:
       ```css
       @keyframes <name> {
         from { opacity: 1; /* final state matching the "to" of the original keyframe */ }
         to   { opacity: 1; /* same */ }
       }
       ```

    Step 3 — Confirm globals.css structure is intact:
    Read globals.css and verify:
    - `@keyframes reveal-up` block exists at lines ~29-32
    - `@media (prefers-reduced-motion: reduce)` block exists immediately after at lines ~34-39
    - The override keyframe sets `from { opacity: 1; transform: translateY(0); }` (no animation)

    Step 4 — If the scan confirms animate-reveal-up is the only entrance animation (the expected case):
    Make NO changes to globals.css. Record the audit result in the task output.

    Step 5 — Confirm CssTimeline.tsx IntersectionObserver logic is unchanged:
    Read CssTimeline.tsx and verify the observer logic (useEffect + IntersectionObserver + `setInView(true)`) is identical to what was built in Phase 1. Do NOT modify it.

    IMPORTANT: Do NOT add `motion-reduce:` Tailwind prefix to any component class string anywhere (per D-02 and D-03). The keyframe-override approach in globals.css is the sole mechanism.
  </action>
  <verify>
    <automated>grep -rn "animate-" /Users/nathanlieberman/Documents/GWRepos/tailwind-perfect-copilot/src/ --include="*.tsx" --include="*.ts" 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - The grep above returns ONLY lines from `src/components/CssTimeline.tsx` containing `animate-reveal-up`
    - OR if additional animate-* classes are found: each one is either (a) a Tailwind built-in (animate-spin/pulse/bounce — no action needed) or (b) a custom entrance animation that has been added to the globals.css reduced-motion block
    - `grep -c "prefers-reduced-motion" src/app/globals.css` returns `1`
    - `grep -A5 "prefers-reduced-motion" src/app/globals.css` shows the `@keyframes reveal-up` override with `from { opacity: 1; transform: translateY(0); }`
    - No `motion-reduce:` class prefix appears anywhere in any .tsx or .ts file: `grep -rn "motion-reduce:" src/` returns zero lines
    - No changes made to CssTimeline.tsx IntersectionObserver logic
    - No changes made to any component's transition-colors, transition-all, or transition-transform classes
  </acceptance_criteria>
  <done>
    Reduced-motion audit complete. Either: (a) confirmed animate-reveal-up is the only entrance animation and it is already gated — no changes needed, or (b) any additional entrance animations found have been added to the globals.css reduced-motion block. The @media (prefers-reduced-motion: reduce) block is complete and sufficient.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| globals.css → browser | CSS file served statically — no runtime trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Denial of Service | prefers-reduced-motion audit | accept | Read-only audit of static CSS/TSX files — no user input, no network calls, no attack surface |
| T-07-04 | Information Disclosure | None applicable | accept | CSS audit only — no secrets or user data involved |
</threat_model>

<verification>
- `grep -rn "animate-" src/ --include="*.tsx" --include="*.ts"` returns only CssTimeline.tsx
- `grep -c "prefers-reduced-motion" src/app/globals.css` returns `1`
- `grep -rn "motion-reduce:" src/` returns zero lines
</verification>

<success_criteria>
- Confirmed: animate-reveal-up is the only entrance animation in the codebase
- Confirmed: the globals.css @media (prefers-reduced-motion: reduce) block already gates it with a no-op keyframe override
- No motion-reduce: Tailwind prefixes added to any component (those are interactive transitions, not entrance animations)
- CssTimeline IntersectionObserver logic unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/07-tv-readability-quality-pass/07-02-SUMMARY.md` documenting:
- Complete list of animate-* classes found (expected: only animate-reveal-up in CssTimeline.tsx)
- Whether globals.css was modified (expected: no changes needed)
- Confirmation that no motion-reduce: prefixes were added
</output>
