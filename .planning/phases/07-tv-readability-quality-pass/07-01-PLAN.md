---
phase: 07-tv-readability-quality-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/CssTimeline.tsx
  - src/components/SlideLayout.tsx
  - src/components/CodeCallout.tsx
  - src/components/ButtonComparison.tsx
  - src/components/CardBuilder.tsx
  - src/components/FlexGridComparison.tsx
  - src/components/ResponsiveDemo.tsx
  - src/components/ConditionalPanels.tsx
  - src/components/DataActiveDemo.tsx
  - src/app/what-is-tailwind/page.tsx
  - src/app/utility-classes/page.tsx
  - src/app/responsiveness-dark-mode/page.tsx
  - src/app/customizing-tailwind/page.tsx
  - src/app/conditional-styling/page.tsx
autonomous: true
requirements:
  - QA-01

must_haves:
  truths:
    - "Every text element on all 6 slides is legible at 1920px (3xl breakpoint) without zooming"
    - "All overline labels have text-xs escalated to 3xl:text-sm or 3xl:text-base"
    - "All body/description paragraphs with text-sm have a 3xl: escalation"
    - "All section headings with text-xl have a 3xl: escalation"
    - "SlideLayout nav chrome (back link, slide number badge) has 3xl: escalation"
    - "Demo content class strings in lookup-table maps (STEP_STYLES, LAYOUT_CLASSES, etc.) are unchanged"
  artifacts:
    - path: "src/components/SlideLayout.tsx"
      provides: "Shared nav — back link and badge already escalated (confirmed in pre-read)"
    - path: "src/components/CssTimeline.tsx"
      provides: "Slide 1 timeline — already has 3xl: and 4xl: escalations; confirm no gaps in detail paragraphs"
    - path: "src/components/CodeCallout.tsx"
      provides: "Shared callout — already has 3xl:text-base (confirmed in pre-read)"
  key_links:
    - from: "all slide page.tsx files"
      to: "3xl: escalation on every text-xs / text-sm / text-base occurrence"
      via: "direct className attribute"
      pattern: "3xl:text-"
---

<objective>
TV readability audit and fix pass across all 6 slide pages and shared components.

Purpose: The presentation is shown on a TV at 1920px. Every text element must be legible from viewing distance. This plan scans every component and page file for text-xs, text-sm, and text-base classes that lack a 3xl: escalation and adds the missing escalations.

Output: All source files patched such that no unescalated small text survives in slide UI chrome. Demo content inside lookup-table maps (STEP_STYLES in CardBuilder, LAYOUT_CLASSES in ResponsiveDemo) is intentionally left unchanged — those class strings ARE the demo content.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Key escalation ratios established in prior phases (from 07-CONTEXT.md) -->
<!-- Follow these ratios exactly — do not invent new escalation levels -->

text-xs  → 3xl:text-sm  (overline labels, badge text, small UI chrome)
text-xs  → 3xl:text-base (overline labels used as primary labels, per prior pattern)
text-sm  → 3xl:text-base or 3xl:text-lg or 3xl:text-xl (body text, depends on context)
text-base → 3xl:text-xl or 3xl:text-2xl (section subheadings or card headings)
text-xl  → 3xl:text-3xl (section h2 headings — already established in page files)

spacing escalations (for reference, do not change ones already present):
gap-6    → 3xl:gap-12
p-6      → 3xl:p-8 or 3xl:p-10
px-8     → 3xl:px-16
py-4     → 3xl:py-6

<!-- FILES ALREADY CONFIRMED COMPLIANT (no changes needed) -->
<!-- SlideLayout.tsx: back link has 3xl:text-base, badge has 3xl:text-sm, h1 has 3xl:text-7xl -->
<!-- CodeCallout.tsx: has 3xl:text-base, 3xl:px-4, 3xl:py-2, 3xl:rounded-xl -->
<!-- CssTimeline.tsx: fully escalated with 3xl: and 4xl: on all text elements -->
<!-- ButtonComparison.tsx: TAILWIND_BTN_CLASSES const has 3xl:text-xl -->
<!-- FlexGridComparison.tsx: overlines have 3xl:text-base, box labels have 3xl:text-base -->
<!-- ResponsiveDemo.tsx: tabs have 3xl:text-lg, profile names have 3xl:text-base, roles have 3xl:text-sm, overline has 3xl:text-base -->
<!-- CardBuilder.tsx: navigator buttons 3xl:text-lg, step nodes 3xl:text-xl, step labels 3xl:text-sm, overlines 3xl:text-base -->
<!-- what-is-tailwind/page.tsx: overlines 3xl:text-base, body 3xl:text-xl, card heading 3xl:text-2xl -->
<!-- utility-classes/page.tsx: h2s have 3xl:text-3xl, bodies have 3xl:text-xl -->
<!-- responsiveness-dark-mode/page.tsx: h2s 3xl:text-3xl, bodies 3xl:text-xl, overlines 3xl:text-base, badge 3xl:text-sm -->
<!-- customizing-tailwind/page.tsx: overlines 3xl:text-base, body paras 3xl:text-lg -->
<!-- ConditionalPanels.tsx: OVERLINE const has 3xl:text-base, SUBLABEL has 3xl:text-sm, body texts 3xl:text-xl -->
<!-- DataActiveDemo.tsx: OVERLINE has 3xl:text-base, body has 3xl:text-xl, button has 3xl:text-base -->
<!-- conditional-styling/page.tsx: OVERLINE const has 3xl:text-base, labels 3xl:text-base, input 3xl:text-base, validation 3xl:text-sm -->

<!-- CONFIRMED GAPS (items that DO need fixing) -->
<!-- ShikiBlock.tsx: has 3xl:text-base — confirmed compliant -->
<!-- All pages: confirmed compliant from pre-read grep pass -->

<!-- LOOKUP-TABLE MAPS — DO NOT ESCALATE THESE (per D-07) -->
<!-- CardBuilder STEPS[].allClasses and .newClasses — intentional demo content -->
<!-- CardBuilder STEPS[].label strings — rendered as step names, already small -->
<!-- ResponsiveDemo LAYOUT_CLASSES and CALLOUT_CLASSES maps — demo content -->
<!-- ConditionalPanels PANEL3_CLASSES map — demo content -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Systematic 3xl: audit scan across all slide files and components</name>
  <files>
    src/components/CssTimeline.tsx
    src/components/SlideLayout.tsx
    src/components/CodeCallout.tsx
    src/components/ButtonComparison.tsx
    src/components/CardBuilder.tsx
    src/components/FlexGridComparison.tsx
    src/components/ResponsiveDemo.tsx
    src/components/ConditionalPanels.tsx
    src/components/DataActiveDemo.tsx
    src/components/ShikiBlock.tsx
    src/app/what-is-tailwind/page.tsx
    src/app/utility-classes/page.tsx
    src/app/responsiveness-dark-mode/page.tsx
    src/app/customizing-tailwind/page.tsx
    src/app/conditional-styling/page.tsx
  </files>
  <read_first>
    - src/components/CssTimeline.tsx (Slide 1 — has 3xl: and 4xl: escalations throughout; confirm detail paragraph body text is covered)
    - src/components/SlideLayout.tsx (nav back link, badge — confirm 3xl:text-base and 3xl:text-sm already present)
    - src/components/CodeCallout.tsx (confirm 3xl:text-base present)
    - src/components/ButtonComparison.tsx (confirm TAILWIND_BTN_CLASSES has 3xl:text-xl)
    - src/components/CardBuilder.tsx (step navigator chrome — do NOT touch STEPS[].allClasses or .newClasses)
    - src/components/FlexGridComparison.tsx (BOXES className strings in BOXES array are demo content — do NOT escalate)
    - src/components/ResponsiveDemo.tsx (LAYOUT_CLASSES and CALLOUT_CLASSES maps are demo content — do NOT escalate)
    - src/components/ConditionalPanels.tsx (PANEL3_CLASSES map is demo content — do NOT escalate; GROUP_CALLOUT/STATE_CALLOUT are demo content — do NOT escalate)
    - src/components/DataActiveDemo.tsx (DATA_CALLOUT const is demo content — do NOT escalate)
    - src/components/ShikiBlock.tsx (confirm 3xl:text-base on the wrapper div)
    - src/app/what-is-tailwind/page.tsx
    - src/app/utility-classes/page.tsx
    - src/app/responsiveness-dark-mode/page.tsx
    - src/app/customizing-tailwind/page.tsx
    - src/app/conditional-styling/page.tsx
  </read_first>
  <action>
    Read each file listed above. For each file:

    1. Search for every occurrence of `text-xs`, `text-sm`, `text-base` in className attributes or const strings that are UI chrome (NOT demo content inside lookup-table maps).

    2. For each occurrence, check if a `3xl:text-*` escalation already exists on the same element's className string. If it does, skip it.

    3. If a `3xl:text-*` escalation is missing, add it following the established ratios:
       - `text-xs` in overline labels → add `3xl:text-base` (matching the overline pattern used in all pages)
       - `text-xs` in badge/chip elements → add `3xl:text-sm`
       - `text-xs` in validation messages → add `3xl:text-sm`
       - `text-sm` in body paragraphs → add `3xl:text-xl` (matching body text pattern in all pages)
       - `text-sm` in button labels → add `3xl:text-base` or `3xl:text-lg` depending on button prominence
       - `text-sm` in card subtext → add `3xl:text-xl`
       - `text-base` in card headings → add `3xl:text-2xl`

    4. EXEMPT — Do NOT add 3xl: escalations to:
       - Any string that is a value in a lookup-table map used as demo content: CardBuilder STEPS[].allClasses and STEPS[].newClasses, ResponsiveDemo LAYOUT_CLASSES and CALLOUT_CLASSES, ConditionalPanels PANEL3_CLASSES, DataActiveDemo DATA_CALLOUT, ButtonComparison BTN_CSS_DEFINITION
       - The BOXES array className strings in FlexGridComparison (those ARE the demo content the audience sees)
       - The FLEX_CLASSES and GRID_CLASSES consts in FlexGridComparison (those are CodeCallout content)

    5. After reading all files, make only the edits required. Based on the pre-read grep pass, most files are already compliant. Apply fixes only where escalations are genuinely missing.

    Focus areas most likely to have gaps (from CONTEXT.md specifics):
    - CssTimeline detail paragraphs inside expanded accordion sections (body text inside `<div className="px-6 pb-6...">` and `<li className="flex items-start gap-2 text-sm...">`)
    - Any text-sm or text-xs in page.tsx files that doesn't have a 3xl: peer on the same className string
  </action>
  <verify>
    <automated>grep -rn "text-xs\|text-sm\|text-base" src/components/CssTimeline.tsx src/components/SlideLayout.tsx src/components/CodeCallout.tsx src/components/ButtonComparison.tsx src/components/CardBuilder.tsx src/components/FlexGridComparison.tsx src/components/ResponsiveDemo.tsx src/components/ConditionalPanels.tsx src/components/DataActiveDemo.tsx src/app/what-is-tailwind/page.tsx src/app/utility-classes/page.tsx src/app/responsiveness-dark-mode/page.tsx src/app/customizing-tailwind/page.tsx src/app/conditional-styling/page.tsx 2>/dev/null | grep -v "3xl:" | grep -v "STEP_STYLES\|allClasses\|newClasses\|LAYOUT_CLASSES\|CALLOUT_CLASSES\|PANEL3_CLASSES\|DATA_CALLOUT\|BTN_CSS_DEFINITION\|FLEX_CLASSES\|GRID_CLASSES\|BOXES\b"</automated>
  </verify>
  <acceptance_criteria>
    - The automated grep command above returns zero lines OR only lines that are inside exempt lookup-table map const strings (STEP_STYLES, LAYOUT_CLASSES, etc.)
    - Every `text-xs` occurrence in overline labels has a `3xl:text-base` or `3xl:text-sm` on the same className string
    - Every `text-sm` occurrence in body paragraphs/descriptions has a `3xl:text-xl` or `3xl:text-lg` or `3xl:text-base` on the same className string
    - No changes made to CardBuilder STEPS array class strings, ResponsiveDemo LAYOUT_CLASSES/CALLOUT_CLASSES map, ConditionalPanels PANEL3_CLASSES map, or DataActiveDemo DATA_CALLOUT const
    - Confirmed by reading file after edit: `grep -c "3xl:text-" src/components/CssTimeline.tsx` returns at least 10 (already present — confirm unchanged)
  </acceptance_criteria>
  <done>
    All 14 component/page files scanned. Every UI chrome text element (overlines, body paragraphs, labels, badges, button text) that used text-xs/text-sm/text-base has a corresponding 3xl: escalation. Lookup-table map class strings left unchanged.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    3xl: audit and fix pass across all 6 slide pages and shared components. All UI chrome text escalated for TV readability at 1920px.
  </what-built>
  <how-to-verify>
    1. Run `yarn dev` if not already running.
    2. Open Chrome DevTools → Toggle device toolbar → set width to 1920px (Custom device or manually type in width field).
    3. Visit each slide in sequence and confirm ALL text is legible without zooming:
       - http://localhost:3000/history-of-css — expand at least 2 timeline entries, confirm milestone lists and body text are readable
       - http://localhost:3000/what-is-tailwind — confirm overlines, body paragraphs, card heading all readable
       - http://localhost:3000/utility-classes — confirm step navigator labels, section headings, body paragraphs readable
       - http://localhost:3000/responsiveness-dark-mode — confirm section headings, body paragraphs, badge, button labels readable
       - http://localhost:3000/customizing-tailwind — confirm overlines, body paragraphs, ShikiBlock text readable
       - http://localhost:3000/conditional-styling — confirm panel overlines, sublabels, form validation message, body paragraphs readable
    4. Also toggle dark mode on each slide and confirm text remains legible.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 slides are legible at 1920px, or describe which elements are too small and on which slide.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source files → browser | Static className strings compiled by Tailwind — no runtime trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Demo content consts (STEP_STYLES, LAYOUT_CLASSES) | accept | Read-only audit — lookup-table map strings are intentionally unchanged; no user input processed |
| T-07-02 | Information Disclosure | None applicable | accept | Phase is CSS class audit only — no secrets, no user data, no API calls |
</threat_model>

<verification>
After Task 1 completes:
- `grep -rn "text-xs\|text-sm\|text-base" src/components/ src/app/*/page.tsx 2>/dev/null | grep -v "3xl:" | grep -v "STEP_STYLES\|allClasses\|newClasses\|LAYOUT_CLASSES\|CALLOUT_CLASSES\|PANEL3_CLASSES\|DATA_CALLOUT\|BTN_CSS_DEFINITION\|FLEX_CLASSES\|GRID_CLASSES"` returns zero lines outside of exempt map consts

After checkpoint:
- Human confirmed all 6 slides legible at 1920px
</verification>

<success_criteria>
- All UI chrome text elements (overlines, body paragraphs, card headings, badge text, button labels, form labels, validation messages) on all 6 slides have 3xl: size escalations
- Demo content inside lookup-table maps is untouched (those class strings are the content being demonstrated, not UI chrome)
- Human spot-check at 1920px confirms legibility
</success_criteria>

<output>
After completion, create `.planning/phases/07-tv-readability-quality-pass/07-01-SUMMARY.md` documenting:
- Which files were modified and what escalations were added
- Which files were already compliant (no changes needed)
- The human verification result
</output>
