---
phase: 05-slide-5-customizing-tailwind
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/app/customizing-tailwind/page.tsx
  - src/test/app/slide-pages.test.tsx
autonomous: true
requirements:
  - S5-01
  - S5-02
  - S5-03

must_haves:
  truths:
    - "Visiting /customizing-tailwind renders three stacked sections: @theme, @utility, @layer base"
    - "Section 1 shows a Shiki-highlighted @theme snippet AND a live `bg-brand-500` swatch (S5-01)"
    - "Section 2 shows a Shiki-highlighted @utility snippet AND a CodeCallout naming the v3 equivalent (S5-02)"
    - "Section 3 shows a Shiki-highlighted @layer base snippet AND a live <h1> demo with no extra class (S5-03)"
    - "yarn test --run passes (stub assertion removed for slide 05; new content assertions pass)"
    - "yarn build exits 0 (no purged classes; bg-brand-500 generated)"
  artifacts:
    - path: "src/app/customizing-tailwind/page.tsx"
      provides: "Three-section RSC slide page replacing the stub"
      contains: "ShikiBlock"
      min_lines: 70
    - path: "src/test/app/slide-pages.test.tsx"
      provides: "Updated stubCases filter (no '05') and new content-presence assertions for S5-02 and S5-03"
      contains: "Customizing Tailwind content"
  key_links:
    - from: "src/app/customizing-tailwind/page.tsx"
      to: "src/components/ShikiBlock.tsx"
      via: "import ShikiBlock from '@/components/ShikiBlock'"
      pattern: "import ShikiBlock from ['\"]@/components/ShikiBlock['\"]"
    - from: "src/app/customizing-tailwind/page.tsx"
      to: "src/components/CodeCallout.tsx"
      via: "import CodeCallout from '@/components/CodeCallout' (Section 2 v3 equivalent)"
      pattern: "<CodeCallout classes=\\{V3_EQUIVALENT\\}"
    - from: "src/app/customizing-tailwind/page.tsx"
      to: "src/app/globals.css plain @theme --color-brand-500 token"
      via: "Tailwind utility class `bg-brand-500` rendered on the live swatch"
      pattern: "bg-brand-500"
    - from: "src/test/app/slide-pages.test.tsx"
      to: "src/app/customizing-tailwind/page.tsx"
      via: "render(<CustomizingTailwind />) tests pass with real content (no longer stub)"
      pattern: "Customizing Tailwind content"
---

<objective>
Replace the stub at `src/app/customizing-tailwind/page.tsx` with the full three-section slide layout (S5-01, S5-02, S5-03), and update `src/test/app/slide-pages.test.tsx` to remove `"05"` from `stubCases` and add content-presence assertions for the new sections. Run `yarn test --run && yarn build` as the final phase gate.

Purpose: This is the user-visible payload of Phase 5. The first two plans set up infrastructure (Shiki mock + ShikiBlock component, globals.css token + utility); this plan wires them together into the actual slide. After this plan, an attendee viewing `/customizing-tailwind` sees the complete teaching artifact described in ROADMAP.md Phase 5 success criteria.

Output:
- `src/app/customizing-tailwind/page.tsx` — full RSC slide page implementing UI-SPEC §Layout Contract verbatim
- `src/test/app/slide-pages.test.tsx` — `"05"` removed from `stubCases`; new `describe("Customizing Tailwind content", ...)` block with two `it()` assertions for the @utility and @layer base snippets
- `yarn test --run` exits 0
- `yarn build` exits 0
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-slide-5-customizing-tailwind/05-CONTEXT.md
@.planning/phases/05-slide-5-customizing-tailwind/05-RESEARCH.md
@.planning/phases/05-slide-5-customizing-tailwind/05-PATTERNS.md
@.planning/phases/05-slide-5-customizing-tailwind/05-UI-SPEC.md
@.planning/phases/05-slide-5-customizing-tailwind/05-VALIDATION.md
@.planning/phases/05-slide-5-customizing-tailwind/05-01-SUMMARY.md
@.planning/phases/05-slide-5-customizing-tailwind/05-02-SUMMARY.md
@src/app/customizing-tailwind/page.tsx
@src/components/SlideLayout.tsx
@src/components/CodeCallout.tsx
@src/components/ShikiBlock.tsx
@src/app/responsiveness-dark-mode/page.tsx
@src/app/what-is-tailwind/page.tsx
@src/test/app/slide-pages.test.tsx
@src/app/globals.css

<interfaces>
<!-- Contracts from upstream plans this plan consumes. -->

From `src/components/ShikiBlock.tsx` (Plan 01 output):
```tsx
interface ShikiBlockProps {
  code: string;
  lang: "css" | "typescript" | "javascript";
}
export default async function ShikiBlock(props: Readonly<ShikiBlockProps>): Promise<JSX.Element>;
```
- Async RSC. Render with `<ShikiBlock code={CONST} lang="css" />`.
- The async-ness propagates: any component that renders ShikiBlock must itself be allowed to be async (an RSC page is fine; client components are not).

From `src/components/CodeCallout.tsx` (existing):
```tsx
interface CodeCalloutProps { classes: string; }
export default function CodeCallout({ classes }: Readonly<CodeCalloutProps>): JSX.Element;
```

From `src/components/SlideLayout.tsx` (existing):
- Wraps slide content; provides nav bar, slide-number badge, and `<h1>` rendered with the global `@layer base` gradient.
- Used as `<SlideLayout number="05" title="Customizing Tailwind">{children}</SlideLayout>`.

From `src/app/globals.css` (Plan 02 output):
- Token `--color-brand-500: #3b82f6` is in the plain `@theme {}` block, so `bg-brand-500` is a real Tailwind utility.
- Top-level rule `@utility scrollbar-hidden { scrollbar-width: none; }` exists.

From `src/test/app/slide-pages.test.tsx` (existing — to be edited):
```tsx
const stubCases = allCases.filter(({ number }) =>
  ["04", "05", "06"].includes(number)        // ← change to ["06"] (04 is also done; 05 done now)
);
```
Note: Phase 4 is complete per STATE.md and the latest git history; therefore both `"04"` and `"05"` must be removed from `stubCases`. The original plan was to only drop `"05"`, but `"04"` is also stale and the existing test file currently asserts incorrectly that slide 04 shows "Content coming soon". See acceptance criteria below for exact post-edit expression.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace customizing-tailwind/page.tsx stub with the three-section slide</name>
  <files>src/app/customizing-tailwind/page.tsx</files>
  <read_first>
    - src/app/customizing-tailwind/page.tsx (current stub — confirm it is the 5-line stub before replacing)
    - src/components/SlideLayout.tsx (wrapper contract)
    - src/components/CodeCallout.tsx (used in Section 2 right column)
    - src/components/ShikiBlock.tsx (Plan 01 output — used in all three sections)
    - src/app/responsiveness-dark-mode/page.tsx (Phase 4 stacked-section + spacer pattern)
    - src/app/what-is-tailwind/page.tsx (two-column grid pattern: `grid grid-cols-2 gap-6 3xl:gap-12`)
    - src/app/globals.css (confirm `--color-brand-500` and `@utility scrollbar-hidden` are present from Plan 02)
    - .planning/phases/05-slide-5-customizing-tailwind/05-UI-SPEC.md (§Layout Contract, §Static String Constants, §Copywriting Contract, §TV Readability Contract)
    - .planning/phases/05-slide-5-customizing-tailwind/05-RESEARCH.md (§Code Examples — Page Structure Skeleton)
    - .planning/phases/05-slide-5-customizing-tailwind/05-CONTEXT.md (D-04, D-05, D-06, D-07)
  </read_first>
  <behavior>
    - Test 1 (rendered output, S5-01 verified by build): The page renders `<div className="bg-brand-500" />` and `yarn build` succeeds — confirming the brand-500 utility is generated and the static class string is detected by Tailwind.
    - Test 2 (rendered output, S5-02 — added in Task 2 of this plan): The page renders text containing `scrollbar-hidden` (in the Shiki snippet AND in the v3 equivalent CodeCallout) — verified by Vitest in Task 2.
    - Test 3 (rendered output, S5-03 — added in Task 2 of this plan): The page renders text containing `@layer base` (in the Shiki snippet) AND a live `<h1>` element with text "Live h1 Demo" — verified by Vitest in Task 2.
    - Test 4 (smoke, this task): `yarn test --run` continues to pass DURING this task even though the stubCases filter still includes "05" — because the BASE_SNIPPET contains the literal text "Customizing Tailwind"... wait, no: the existing stub assertion will FAIL once the stub is replaced because "Content coming soon" is gone. Therefore THIS task MUST be done together with Task 2, OR the test file must be updated FIRST. Per the plan structure here, Task 2 immediately follows Task 1 in the same plan — so the executor MUST run `yarn test --run` only AFTER Task 2 is done. This task's verify is intentionally a build-only check.
  </behavior>
  <action>
    Replace the entire contents of `src/app/customizing-tailwind/page.tsx` with EXACTLY the following code. Do not add `"use client"`. Do not add any imports beyond the three shown. Do not change any string content (overlines, captions, snippet constants).

    ```tsx
    import SlideLayout from "@/components/SlideLayout";
    import CodeCallout from "@/components/CodeCallout";
    import ShikiBlock from "@/components/ShikiBlock";

    const THEME_SNIPPET = `@theme {
      --color-brand-500: #3b82f6;
    }`;

    const UTILITY_SNIPPET = `@utility scrollbar-hidden {
      scrollbar-width: none;
    }`;

    const BASE_SNIPPET = `@layer base {
      h1 {
        @apply font-bold tracking-tight text-transparent
               bg-clip-text bg-linear-to-r
               from-slate-900 to-slate-500
               dark:from-white dark:to-slate-400;
      }
    }`;

    const V3_EQUIVALENT = `@layer utilities {
      .scrollbar-hidden {
        scrollbar-width: none;
      }
    }`;

    export default function CustomizingTailwind() {
      return (
        <SlideLayout number="05" title="Customizing Tailwind">
          {/* Section 1 — @theme */}
          <section>
            <p className="text-xs font-semibold tracking-widest uppercase text-cyan-600 dark:text-cyan-400 3xl:text-base">
              CSS-FIRST CONFIG
            </p>
            <div className="mt-8 grid grid-cols-2 gap-6 3xl:gap-12 3xl:mt-12">
              <ShikiBlock code={THEME_SNIPPET} lang="css" />
              <div>
                <div className="h-16 w-full rounded-xl bg-brand-500 3xl:h-24" />
                <p className="mt-3 text-sm text-slate-600 dark:text-slate-400 3xl:text-lg">
                  bg-brand-500 — auto-generated from the token above
                </p>
              </div>
            </div>
          </section>

          <div className="mt-16 3xl:mt-24" />

          {/* Section 2 — @utility */}
          <section>
            <p className="text-xs font-semibold tracking-widest uppercase text-cyan-600 dark:text-cyan-400 3xl:text-base">
              CUSTOM UTILITIES
            </p>
            <div className="mt-8 grid grid-cols-2 gap-6 3xl:gap-12 3xl:mt-12">
              <ShikiBlock code={UTILITY_SNIPPET} lang="css" />
              <div>
                <p className="text-xs font-semibold tracking-widest uppercase text-cyan-600 dark:text-cyan-400 3xl:text-base mb-2">
                  V3 EQUIVALENT
                </p>
                <CodeCallout classes={V3_EQUIVALENT} />
              </div>
            </div>
          </section>

          <div className="mt-16 3xl:mt-24" />

          {/* Section 3 — @layer base */}
          <section>
            <p className="text-xs font-semibold tracking-widest uppercase text-cyan-600 dark:text-cyan-400 3xl:text-base">
              BASE LAYER RULES
            </p>
            <div className="mt-8 grid grid-cols-2 gap-6 3xl:gap-12 3xl:mt-12">
              <ShikiBlock code={BASE_SNIPPET} lang="css" />
              <div>
                <h1 className="text-3xl 3xl:text-5xl">Live h1 Demo</h1>
                <p className="mt-3 text-sm text-slate-600 dark:text-slate-400 3xl:text-lg">
                  No class needed — this h1 inherits the gradient from @layer base.
                </p>
              </div>
            </div>
          </section>
        </SlideLayout>
      );
    }
    ```

    Critical implementation notes (do NOT deviate):
    - NO `"use client"` directive — the page is a pure RSC. ShikiBlock is `async` and would not work as a client component.
    - The function `CustomizingTailwind` is NOT marked `async` here even though it renders `ShikiBlock` (an async RSC). Server Components can render other server components — including async ones — as children without needing to be async themselves; React handles the `await` at the framework level. This matches Next.js App Router convention.
    - `bg-brand-500` is used as a complete static class string. Do NOT use any dynamic interpolation like `` `bg-${color}-500` ``.
    - All four named consts (`THEME_SNIPPET`, `UTILITY_SNIPPET`, `BASE_SNIPPET`, `V3_EQUIVALENT`) are at module scope ABOVE the `export default function CustomizingTailwind()` declaration.
    - `BASE_SNIPPET` content must match the existing `@layer base h1` rule in `globals.css` byte-for-byte (the Shiki snippet IS the source code of that rule). The exact `@apply` tokens are: `font-bold tracking-tight text-transparent bg-clip-text bg-linear-to-r from-slate-900 to-slate-500 dark:from-white dark:to-slate-400`.
    - The `<h1 className="text-3xl 3xl:text-5xl">Live h1 Demo</h1>` element in Section 3 is INTENTIONALLY a second `<h1>` on the page. SlideLayout already renders the page's primary h1 ("Customizing Tailwind"); this nested h1 is the live demo proving that `@layer base h1` styles apply without any extra class. This is a known and accepted layout choice per UI-SPEC §Layout Contract and CONTEXT.md §Specifics.
    - The `mb-2` class on the V3 EQUIVALENT overline (Section 2 right column) is intentional — it gives breathing room before the CodeCallout chip.

    Do NOT run `yarn test --run` in this task — the existing `stubCases` filter still includes `"05"` and would fail the "Content coming soon" assertion. Task 2 of this plan updates the test file; only after Task 2 is done is `yarn test --run` runnable. This task's verify is `yarn build` (which does not depend on the test filter).
  </action>
  <verify>
    <automated>grep -q '^import SlideLayout from "@/components/SlideLayout";$' src/app/customizing-tailwind/page.tsx &amp;&amp; grep -q '^import CodeCallout from "@/components/CodeCallout";$' src/app/customizing-tailwind/page.tsx &amp;&amp; grep -q '^import ShikiBlock from "@/components/ShikiBlock";$' src/app/customizing-tailwind/page.tsx &amp;&amp; ! grep -q '"use client"' src/app/customizing-tailwind/page.tsx &amp;&amp; grep -q 'const THEME_SNIPPET' src/app/customizing-tailwind/page.tsx &amp;&amp; grep -q 'const UTILITY_SNIPPET' src/app/customizing-tailwind/page.tsx &amp;&amp; grep -q 'const BASE_SNIPPET' src/app/customizing-tailwind/page.tsx &amp;&amp; grep -q 'const V3_EQUIVALENT' src/app/customizing-tailwind/page.tsx &amp;&amp; grep -q 'bg-brand-500' src/app/customizing-tailwind/page.tsx &amp;&amp; grep -q 'CSS-FIRST CONFIG' src/app/customizing-tailwind/page.tsx &amp;&amp; grep -q 'CUSTOM UTILITIES' src/app/customizing-tailwind/page.tsx &amp;&amp; grep -q 'BASE LAYER RULES' src/app/customizing-tailwind/page.tsx &amp;&amp; grep -q 'V3 EQUIVALENT' src/app/customizing-tailwind/page.tsx &amp;&amp; grep -q 'Live h1 Demo' src/app/customizing-tailwind/page.tsx &amp;&amp; grep -c 'mt-16 3xl:mt-24' src/app/customizing-tailwind/page.tsx | grep -q '^2$' &amp;&amp; yarn build</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/customizing-tailwind/page.tsx` contains exactly the three imports listed (SlideLayout, CodeCallout, ShikiBlock). Each `grep -F 'import X from "@/components/X";'` succeeds.
    - File contains NO `"use client"` directive: `grep -F '"use client"' src/app/customizing-tailwind/page.tsx` returns NON-ZERO.
    - All four named consts are present at module scope: `grep -q '^const THEME_SNIPPET'`, `grep -q '^const UTILITY_SNIPPET'`, `grep -q '^const BASE_SNIPPET'`, `grep -q '^const V3_EQUIVALENT'` all return 0.
    - The literal class string `bg-brand-500` appears in the file: `grep -F 'bg-brand-500' src/app/customizing-tailwind/page.tsx` returns 0.
    - All three section overlines are present as plain text: `grep -F 'CSS-FIRST CONFIG'`, `grep -F 'CUSTOM UTILITIES'`, `grep -F 'BASE LAYER RULES'`, and `grep -F 'V3 EQUIVALENT'` all return 0.
    - The Section 3 live h1 demo text "Live h1 Demo" is present: `grep -F 'Live h1 Demo' src/app/customizing-tailwind/page.tsx` returns 0.
    - The Section 1 caption "bg-brand-500 — auto-generated from the token above" is present: `grep -F 'bg-brand-500 — auto-generated from the token above' src/app/customizing-tailwind/page.tsx` returns 0.
    - The Section 3 caption "No class needed — this h1 inherits the gradient from @layer base." is present: `grep -F 'No class needed — this h1 inherits the gradient from @layer base.' src/app/customizing-tailwind/page.tsx` returns 0.
    - Exactly two spacer divs exist between the three sections: `grep -c 'mt-16 3xl:mt-24' src/app/customizing-tailwind/page.tsx` outputs `2`.
    - The file does NOT contain "Content coming soon" (the SlideLayout fallback should NOT appear because `<SlideLayout>{children}</SlideLayout>` has children): `grep -F 'Content coming soon' src/app/customizing-tailwind/page.tsx` returns NON-ZERO.
    - Three `<ShikiBlock` invocations are present: `grep -c '<ShikiBlock' src/app/customizing-tailwind/page.tsx` outputs `3`.
    - One `<CodeCallout` invocation is present (in Section 2 right column): `grep -c '<CodeCallout' src/app/customizing-tailwind/page.tsx` outputs `1`.
    - `yarn build` exits 0. (Tailwind detects the `bg-brand-500` static class because it is a complete literal in the source.)
  </acceptance_criteria>
  <done>
    The slide page is a complete RSC implementation matching UI-SPEC §Layout Contract: three sections, two spacer divs, four named const snippets, three ShikiBlock instances, one CodeCallout, one live h1 demo, one live `bg-brand-500` swatch. `yarn build` exits 0. Tests are NOT run yet — Task 2 of this plan updates the test file before any test invocation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update slide-pages.test.tsx — drop stale stub cases and add content assertions</name>
  <files>src/test/app/slide-pages.test.tsx</files>
  <read_first>
    - src/test/app/slide-pages.test.tsx (current state — confirm `stubCases` filter and existing describe/it style)
    - src/app/customizing-tailwind/page.tsx (post-Task-1 state — confirms BASE_SNIPPET contains "@layer base" and UTILITY_SNIPPET contains "scrollbar-hidden")
    - src/test/mocks/shiki.ts (Plan 01 output — confirms shiki imports resolve to the mock under Vitest, so `render(<CustomizingTailwind />)` is safe)
    - .planning/phases/05-slide-5-customizing-tailwind/05-RESEARCH.md (§Pitfall 4 — stubCases must be updated; §Validation Architecture — Phase Requirements → Test Map)
    - .planning/phases/05-slide-5-customizing-tailwind/05-VALIDATION.md (§Wave 0 Requirements — second item)
    - .planning/phases/05-slide-5-customizing-tailwind/05-PATTERNS.md (§slide-pages.test.tsx self-modification — exact change spec)
  </read_first>
  <behavior>
    - Test 1: After this task, `yarn test --run` exits 0. The slide-page tests for `CustomizingTailwind` (slide number "05" badge present, h1 "Customizing Tailwind" present) continue to pass — those are unchanged. The "Content coming soon" assertion is no longer applied to slide 05 (or 04).
    - Test 2 (S5-02 assertion): A new test in this file renders `<CustomizingTailwind />` and asserts the screen contains text matching `/scrollbar-hidden/i` — this is satisfied because both the UTILITY_SNIPPET (rendered into the Shiki mock as `<pre class="shiki"><code>MOCK</code></pre>`) AND the V3_EQUIVALENT CodeCallout (rendered as `<p>...</p>`) put `scrollbar-hidden` text into the DOM. The CodeCallout path is what will satisfy the test under the mock (since the Shiki mock returns "MOCK" instead of real text). Therefore the assertion explicitly relies on the CodeCallout text content, which is real DOM text.
    - Test 3 (S5-03 assertion): A new test renders `<CustomizingTailwind />` and asserts the screen contains text matching `/Live h1 Demo/i` — the literal h1 text in Section 3. This avoids the Shiki mock by asserting against the live JSX `<h1>` element, which is rendered as real DOM text. (The original spec suggested asserting against `/@layer base/i` from the snippet, but since Shiki is mocked to return "MOCK", that text is not in the DOM during tests; the live h1 demo text is the reliable assertion.)
  </behavior>
  <action>
    Edit `src/test/app/slide-pages.test.tsx` with TWO precise changes. Do not modify any other lines.

    ### Change 1 — Update the `stubCases` filter

    The current filter (lines 22–24) is:

    ```tsx
    const stubCases = allCases.filter(({ number }) =>
      ["04", "05", "06"].includes(number)
    );
    ```

    Replace it with:

    ```tsx
    const stubCases = allCases.filter(({ number }) =>
      ["06"].includes(number)
    );
    ```

    Rationale: Phase 4 (slide 04) and Phase 5 (slide 05) are now both complete. Their pages render real content, not "Content coming soon". Slide 06 remains a stub (Phase 6 not yet started).

    ### Change 2 — Add a new `describe` block at the END of the file

    Append the following block AFTER the closing `});` of the existing top-level `describe("Slide pages", ...)` block (which currently ends at line 51). Insert exactly one blank line between the two top-level describe blocks.

    ```tsx

    describe("Customizing Tailwind content", () => {
      it("renders the @utility scrollbar-hidden v3 equivalent callout (S5-02)", () => {
        render(<CustomizingTailwind />);
        expect(screen.getByText(/scrollbar-hidden/i)).toBeInTheDocument();
      });

      it("renders the live h1 demo for the @layer base section (S5-03)", () => {
        render(<CustomizingTailwind />);
        expect(screen.getByText(/Live h1 Demo/i)).toBeInTheDocument();
      });
    });
    ```

    Implementation notes:
    - Reuse the existing imports at the top of the file (`render`, `screen` from `@testing-library/react`; `describe`, `it`, `expect` from `vitest`; `CustomizingTailwind` from `@/app/customizing-tailwind/page`). Do NOT add new imports — they all already exist.
    - The first new `it()` matches `/scrollbar-hidden/i` — this hits the V3_EQUIVALENT CodeCallout text (`@layer utilities { .scrollbar-hidden { scrollbar-width: none; } }`), which IS real DOM text under jsdom. (The Shiki block's snippet is replaced by the mock's "MOCK" string, so we don't rely on it.)
    - The second new `it()` matches `/Live h1 Demo/i` — this hits the literal `<h1>Live h1 Demo</h1>` in Section 3, which IS real DOM text. This is the S5-03 evidence (the live h1 inheriting the @layer base gradient is rendered without any extra class).
    - Do NOT use `getAllByText` — `getByText` is correct because each match string appears uniquely in the DOM.
    - The two requirement IDs (S5-02, S5-03) are mentioned in the test description strings for traceability with REQUIREMENTS.md.

    After both edits, run the full phase gate:

    ```bash
    yarn test --run && yarn build
    ```

    Both must exit 0.
  </action>
  <verify>
    <automated>grep -q '\[\"06\"\]\.includes(number)' src/test/app/slide-pages.test.tsx &amp;&amp; ! grep -q '\[\"04\", \"05\", \"06\"\]\.includes(number)' src/test/app/slide-pages.test.tsx &amp;&amp; grep -q 'describe("Customizing Tailwind content"' src/test/app/slide-pages.test.tsx &amp;&amp; grep -q 'getByText(/scrollbar-hidden/i)' src/test/app/slide-pages.test.tsx &amp;&amp; grep -q 'getByText(/Live h1 Demo/i)' src/test/app/slide-pages.test.tsx &amp;&amp; grep -q 'S5-02' src/test/app/slide-pages.test.tsx &amp;&amp; grep -q 'S5-03' src/test/app/slide-pages.test.tsx &amp;&amp; yarn test --run &amp;&amp; yarn build</automated>
  </verify>
  <acceptance_criteria>
    - The new `stubCases` filter appears: `grep -F '["06"].includes(number)' src/test/app/slide-pages.test.tsx` returns 0.
    - The OLD filter is gone: `grep -F '["04", "05", "06"].includes(number)' src/test/app/slide-pages.test.tsx` returns NON-ZERO.
    - A new top-level `describe` block exists: `grep -F 'describe("Customizing Tailwind content"' src/test/app/slide-pages.test.tsx` returns 0.
    - Two new `it()` test cases exist with the exact regex matchers:
      - `grep -F 'getByText(/scrollbar-hidden/i)' src/test/app/slide-pages.test.tsx` returns 0.
      - `grep -F 'getByText(/Live h1 Demo/i)' src/test/app/slide-pages.test.tsx` returns 0.
    - Requirement traceability strings are present:
      - `grep -F 'S5-02' src/test/app/slide-pages.test.tsx` returns 0.
      - `grep -F 'S5-03' src/test/app/slide-pages.test.tsx` returns 0.
    - No new imports were added: the imports section is unchanged. Specifically: `head -10 src/test/app/slide-pages.test.tsx` shows the same imports as before (render, screen, describe, it, expect, plus the five page imports). Verified by: `grep -c '^import ' src/test/app/slide-pages.test.tsx` outputs `7` (2 testing-library/vitest imports + 5 page imports — same as before).
    - `yarn test --run` exits 0. Specifically:
      - Existing test "Customizing Tailwind › renders slide number 05" passes.
      - Existing test "Customizing Tailwind › renders the correct page heading" passes.
      - Stub assertion is no longer applied to slide 04 or slide 05 (only to slide 06).
      - New "Customizing Tailwind content" tests both pass.
    - `yarn build` exits 0.
  </acceptance_criteria>
  <done>
    `slide-pages.test.tsx` correctly identifies slide 06 as the only remaining stub, and asserts content presence for the @utility v3 equivalent (S5-02) and the @layer base live h1 demo (S5-03). The combined phase gate `yarn test --run && yarn build` exits 0. All three Phase 5 requirements (S5-01 via build, S5-02 and S5-03 via Vitest) are now verifiable by automated commands.
  </done>
</task>

</tasks>

<verification>
- `yarn test --run` exits 0 — all Slide 5 tests (existing + 2 new) green; slide 04 and 05 no longer asserted as stubs.
- `yarn build` exits 0 — `bg-brand-500` utility class is generated; no purged classes.
- Visiting `/customizing-tailwind` (manual smoke test, NOT part of automated verify but listed in VALIDATION.md §Manual-Only Verifications) shows three sections with Shiki-highlighted CSS on the left and the corresponding live demo / callout on the right.
- ThemeToggle in `SlideLayout` toggles `<html class="dark">`, which the `.dark .shiki, .dark .shiki span { ... !important }` rule inside ShikiBlock responds to.
- `grep -F 'bg-brand-500' src/app/customizing-tailwind/page.tsx` returns 0 (verifies S5-01 Tailwind class wiring).
- `grep -F 'scrollbar-hidden' src/app/customizing-tailwind/page.tsx` returns 0 (verifies S5-02 utility name presence in the v3 callout).
- `grep -F '@layer base' src/app/customizing-tailwind/page.tsx` returns 0 (verifies S5-03 snippet content; even though Shiki mocks the rendering in tests, the source string is in the file).
</verification>

<success_criteria>
- All three phase requirements are met:
  - **S5-01:** Slide shows globals.css `@theme` block (Shiki snippet) alongside a live `bg-brand-500` swatch — rendered token-to-utility pipeline visible.
  - **S5-02:** Slide shows `@utility scrollbar-hidden` (Shiki snippet) and the v3 equivalent CodeCallout — versioning mapping legible.
  - **S5-03:** Slide shows `@layer base { h1 { ... } }` (Shiki snippet) and a live `<h1>Live h1 Demo</h1>` element with no extra class — base-layer inheritance demonstrated.
- Phase gate passes: `yarn test --run && yarn build` exits 0.
- Stub regression cleared: slide 04 and slide 05 are no longer expected to show "Content coming soon".
- The slide page is RSC end-to-end (no `"use client"`); ShikiBlock is async; `bg-brand-500` is a complete static class string.
- All three Phase 5 ROADMAP success criteria are satisfied (verified by the slide content + build + test combination above).
</success_criteria>

<output>
After completion, create `.planning/phases/05-slide-5-customizing-tailwind/05-03-SUMMARY.md` capturing:
- Files created: none (only modifications)
- Files modified: `src/app/customizing-tailwind/page.tsx` (replaced stub with full slide), `src/test/app/slide-pages.test.tsx` (stubCases reduced to `["06"]`; two new content-presence tests added)
- Verification commands run: `yarn test --run` (passed), `yarn build` (passed)
- Three S5-* requirements satisfied — describe how each is evidenced in the rendered slide:
  - S5-01: `bg-brand-500` swatch + THEME_SNIPPET Shiki block side by side
  - S5-02: UTILITY_SNIPPET Shiki block + V3_EQUIVALENT CodeCallout side by side
  - S5-03: BASE_SNIPPET Shiki block + live `<h1>Live h1 Demo</h1>` side by side
- Hand-off notes: Manual smoke test (start `yarn dev`, visit `/customizing-tailwind`, toggle dark mode) recommended before `/gsd-verify-work`. The `.dark .shiki` selector + `!important` is the dark-mode wiring; if the prototype assumption (D-02) fails at runtime in the browser, the fallback is the two-render CSS-toggle approach (RESEARCH.md §Alternatives Considered).
</output>
