---
phase: 05-slide-5-customizing-tailwind
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
autonomous: true
requirements:
  - S5-01
  - S5-02

must_haves:
  truths:
    - "The Tailwind utility class `bg-brand-500` is generated by the build (S5-01 prerequisite)"
    - "The CSS rule `@utility scrollbar-hidden { scrollbar-width: none; }` exists in globals.css (S5-02 source-of-truth)"
    - "yarn build exits 0 with the new token and utility present"
  artifacts:
    - path: "src/app/globals.css"
      provides: "--color-brand-500 token in plain @theme block; @utility scrollbar-hidden rule"
      contains: "--color-brand-500: #3b82f6"
  key_links:
    - from: "src/app/globals.css plain @theme block"
      to: "Tailwind utility class generator (bg-brand-500)"
      via: "--color-brand-500 token"
      pattern: "@theme \\{[^}]*--color-brand-500: #3b82f6"
    - from: "src/app/globals.css"
      to: "Tailwind v4 utility generator (scrollbar-hidden class)"
      via: "@utility directive"
      pattern: "@utility scrollbar-hidden"
---

<objective>
Add the `--color-brand-500` design token and the `@utility scrollbar-hidden` rule to `globals.css`, then verify the production build picks them up.

Purpose: Slide 5's Section 1 (S5-01) renders a live `<div className="bg-brand-500" />` swatch; the only way that class can exist is for the `--color-brand-500` token to live inside the PLAIN `@theme {}` block (NOT `@theme inline {}` — the `inline` keyword suppresses utility class generation per RESEARCH.md Pitfall 2). Slide 5's Section 2 (S5-02) renders the `@utility scrollbar-hidden` rule on screen via Shiki; the rule MUST actually exist in `globals.css` so the slide is showing real, working code (not theatre).

Output:
- `src/app/globals.css` contains `--color-brand-500: #3b82f6;` as the last token inside the existing plain `@theme {}` block (the one with `--breakpoint-3xl`).
- `src/app/globals.css` contains `@utility scrollbar-hidden { scrollbar-width: none; }` as a new top-level rule appended after the plain `@theme {}` block.
- `yarn build` exits 0 with both additions in place.

Independence: This plan modifies ONLY `globals.css`. It does NOT touch `vitest.config.mts`, `src/test/mocks/shiki.ts`, or `src/components/ShikiBlock.tsx`, so it can run in Wave 1 in parallel with Plan 01. Plan 03 (Wave 2) consumes both this plan's CSS and Plan 01's component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-slide-5-customizing-tailwind/05-CONTEXT.md
@.planning/phases/05-slide-5-customizing-tailwind/05-RESEARCH.md
@.planning/phases/05-slide-5-customizing-tailwind/05-PATTERNS.md
@.planning/phases/05-slide-5-customizing-tailwind/05-UI-SPEC.md
@src/app/globals.css

<interfaces>
<!-- Existing globals.css structure that this plan must preserve. -->
<!-- Source of truth: read src/app/globals.css before editing. -->

Current `globals.css` (read at planning time — confirm at execute time):

```css
@import "tailwindcss";

:root {
  --background: #ffffff;
  --foreground: #171717;
}

@custom-variant dark (&:where(.dark, .dark *));

@theme inline {                 /* ← BLOCK A: 'inline' suppresses utility generation */
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --font-sans: var(--font-geist-sans);
  --font-mono: var(--font-geist-mono);
}

body { ... }

@layer base {
  h1 { @apply ... ; }            /* ← UNCHANGED — Section 3 of the slide displays this */
}

@keyframes reveal-up { ... }
@media (prefers-reduced-motion: reduce) { @keyframes reveal-up { ... } }

@theme {                          /* ← BLOCK B: PLAIN @theme — DOES generate utilities */
  --breakpoint-3xl: 1920px;
  --breakpoint-4xl: 2560px;
  --animate-reveal-up: reveal-up 0.7s ease-out both;
}                                /* ← INSERT --color-brand-500 BEFORE this closing brace */
```

CRITICAL: `--color-brand-500` MUST be added to BLOCK B (plain `@theme`), NOT BLOCK A (`@theme inline`). RESEARCH.md Pitfall 2 explains why.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --color-brand-500 to the plain @theme block</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (read the FULL file before editing — must distinguish `@theme inline` from plain `@theme`)
    - .planning/phases/05-slide-5-customizing-tailwind/05-RESEARCH.md (§Pitfall 2 — `@theme inline` vs `@theme`)
    - .planning/phases/05-slide-5-customizing-tailwind/05-CONTEXT.md (D-08)
    - .planning/phases/05-slide-5-customizing-tailwind/05-UI-SPEC.md (§globals.css Modifications)
  </read_first>
  <action>
    Locate the PLAIN `@theme {}` block in `src/app/globals.css`. It is the block that currently reads:

    ```css
    @theme {
      --breakpoint-3xl: 1920px;
      --breakpoint-4xl: 2560px;
      --animate-reveal-up: reveal-up 0.7s ease-out both;
    }
    ```

    DO NOT edit the `@theme inline { ... }` block (the one near the top with `--color-background`, `--color-foreground`, `--font-sans`, `--font-mono`). The `inline` keyword suppresses utility class generation; adding the token there would NOT produce `bg-brand-500`.

    Edit the plain `@theme {}` block so it becomes EXACTLY:

    ```css
    @theme {
      --breakpoint-3xl: 1920px;
      --breakpoint-4xl: 2560px;
      --animate-reveal-up: reveal-up 0.7s ease-out both;
      --color-brand-500: #3b82f6;
    }
    ```

    The new line is `  --color-brand-500: #3b82f6;` (two-space indent matching the existing tokens, semicolon terminator). Insert it as the LAST token before the closing `}`.

    Do NOT modify any other line of `globals.css` in this task. The `@utility` rule is added in Task 2.
  </action>
  <verify>
    <automated>grep -q "^  --color-brand-500: #3b82f6;$" src/app/globals.css &amp;&amp; awk '/^@theme \{/{flag=1} flag &amp;&amp; /--color-brand-500: #3b82f6/{found=1; exit} END{exit !found}' src/app/globals.css &amp;&amp; ! awk '/^@theme inline \{/,/^\}/' src/app/globals.css | grep -q -- '--color-brand-500'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '  --color-brand-500: #3b82f6;' src/app/globals.css` returns 0 (token present with correct two-space indent and semicolon).
    - The token appears INSIDE the plain `@theme {` block — verified by an awk window from `@theme {` (NOT `@theme inline {`) to the next `}`. The script `awk '/^@theme \{/{flag=1} flag && /--color-brand-500: #3b82f6/{found=1; exit} END{exit !found}' src/app/globals.css` exits 0.
    - The token does NOT appear inside `@theme inline { ... }`. The script `awk '/^@theme inline \{/,/^\}/' src/app/globals.css | grep -F -- '--color-brand-500'` returns NON-ZERO (no match).
    - The existing tokens `--breakpoint-3xl: 1920px;`, `--breakpoint-4xl: 2560px;`, and `--animate-reveal-up: reveal-up 0.7s ease-out both;` are still present (no clobbering): three separate `grep -q` calls all succeed.
    - The existing `@layer base { h1 { ... } }` block is unchanged: `grep -F '@layer base' src/app/globals.css` returns 0; `grep -F 'bg-linear-to-r from-slate-900 to-slate-500 dark:from-white dark:to-slate-400' src/app/globals.css` returns 0.
  </acceptance_criteria>
  <done>
    `--color-brand-500: #3b82f6;` is present inside the plain `@theme {}` block, NOT inside `@theme inline {}`. All other globals.css content is byte-identical to before this task. The build verification happens in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add @utility scrollbar-hidden rule and verify yarn build</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (post-Task-1 state — confirms Task 1 completed and the plain @theme block now has 4 tokens)
    - .planning/phases/05-slide-5-customizing-tailwind/05-RESEARCH.md (§Pattern 3 — Tailwind v4 @utility; §Standard Stack)
    - .planning/phases/05-slide-5-customizing-tailwind/05-CONTEXT.md (D-09)
    - .planning/phases/05-slide-5-customizing-tailwind/05-UI-SPEC.md (§globals.css Modifications)
  </read_first>
  <action>
    Append a new top-level CSS rule to `src/app/globals.css` AFTER the closing `}` of the plain `@theme {}` block (i.e., at the very end of the file). The rule must be EXACTLY:

    ```css

    @utility scrollbar-hidden {
      scrollbar-width: none;
    }
    ```

    (Note the leading blank line for readability — match the existing one-blank-line separation between top-level blocks in this file.)

    Use `scrollbar-width: none` (NOT `overflow: hidden`). Per RESEARCH.md Assumption A3, `scrollbar-width: none` is a narrower, cleaner demo that doesn't change box model behavior, and per CONTEXT.md D-09 Claude has discretion to choose this property — the choice is locked here for downstream consistency with the snippet displayed in Slide 5 Section 2.

    The full final state of the bottom of `globals.css` must be:

    ```css
    @theme {
      --breakpoint-3xl: 1920px;
      --breakpoint-4xl: 2560px;
      --animate-reveal-up: reveal-up 0.7s ease-out both;
      --color-brand-500: #3b82f6;
    }

    @utility scrollbar-hidden {
      scrollbar-width: none;
    }
    ```

    After saving, run `yarn build` to verify both the new token and the new utility compile cleanly. The build must:
    - Exit 0
    - NOT emit any warning about unknown `@utility` directive (Tailwind v4 supports this natively)
    - NOT emit any warning about an unrecognized CSS custom property in `@theme`

    If the build fails, do not attempt to "fix" by reverting — diagnose the actual cause (most likely a typo). Both additions are syntactically valid Tailwind v4 per RESEARCH.md Patterns 2 & 3 (verified via Context7).
  </action>
  <verify>
    <automated>grep -q "^@utility scrollbar-hidden {" src/app/globals.css &amp;&amp; grep -q "^  scrollbar-width: none;" src/app/globals.css &amp;&amp; yarn build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '@utility scrollbar-hidden {' src/app/globals.css` returns 0.
    - `grep -F '  scrollbar-width: none;' src/app/globals.css` returns 0.
    - The `@utility` rule appears AFTER the plain `@theme {}` block: line number of `@utility scrollbar-hidden` is greater than line number of `--color-brand-500`. Verified by: `[ "$(grep -n '@utility scrollbar-hidden' src/app/globals.css | head -1 | cut -d: -f1)" -gt "$(grep -n -- '--color-brand-500' src/app/globals.css | head -1 | cut -d: -f1)" ]` exits 0.
    - The `@utility` rule is NOT nested inside `@theme {}` or `@theme inline {}` — it is a top-level block. Verified by: it appears AFTER the plain `@theme {}` block's closing brace.
    - `yarn build` exits 0.
    - The build output does not contain the strings `error` or `Failed` (case-insensitive grep over the build output is acceptable as a soft signal; the exit code is the hard gate).
    - The `--color-brand-500` token from Task 1 is still present (not clobbered): `grep -F '  --color-brand-500: #3b82f6;' src/app/globals.css` returns 0.
  </acceptance_criteria>
  <done>
    `globals.css` ends with the plain `@theme {}` block (containing 4 tokens including `--color-brand-500: #3b82f6;`) followed by a top-level `@utility scrollbar-hidden { scrollbar-width: none; }` rule. `yarn build` exits 0, confirming Tailwind v4 accepts both additions and the `bg-brand-500` utility class is generated for use in Plan 03's slide page.
  </done>
</task>

</tasks>

<verification>
- `yarn build` exits 0 with both additions present.
- `grep -F '  --color-brand-500: #3b82f6;' src/app/globals.css` returns 0.
- `grep -F '@utility scrollbar-hidden {' src/app/globals.css` returns 0.
- `awk '/^@theme inline \{/,/^\}/' src/app/globals.css | grep -F -- '--color-brand-500'` returns NON-ZERO (token NOT in `@theme inline`).
- The `@layer base { h1 { ... } }` rule is unchanged.
- This plan's file footprint is exactly one file: `src/app/globals.css` — no overlap with Plan 01.
</verification>

<success_criteria>
- The Tailwind class `bg-brand-500` is a real, generated utility (verified by clean `yarn build`).
- The `@utility scrollbar-hidden` rule exists as real code in the project's stylesheet (so when Slide 5 displays it via Shiki, the audience sees actual working code, not a fake snippet).
- No regression to existing styles: `--background`, `--foreground`, font-family vars, the `@layer base h1` gradient, breakpoints `--breakpoint-3xl`/`-4xl`, and the `--animate-reveal-up` keyframes-token wiring all unchanged.
- Plan 03 can confidently render `<div className="bg-brand-500" />` knowing the class exists.
</success_criteria>

<output>
After completion, create `.planning/phases/05-slide-5-customizing-tailwind/05-02-SUMMARY.md` capturing:
- Files modified: `src/app/globals.css` (added `--color-brand-500` to plain `@theme` block; appended `@utility scrollbar-hidden { scrollbar-width: none; }`)
- Verification commands run: `yarn build` (exit 0)
- Confirmation that `--color-brand-500` is in the PLAIN `@theme` block, NOT `@theme inline`
- Hand-off notes for Plan 03: `bg-brand-500` is now a real Tailwind utility class; the snippet strings in `page.tsx` should mirror the exact CSS added here (with `scrollbar-width: none`).
</output>
