---
phase: 03-slide-3-core-utility-classes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/FlexGridComparison.tsx
autonomous: true
requirements:
  - S3-02

must_haves:
  truths:
    - "Two containers (FLEX and GRID) are rendered side-by-side with identical numbered box children"
    - "The flex container uses the exact class string 'flex flex-row gap-4' from a named const"
    - "The grid container uses the exact class string 'grid grid-cols-3 gap-4' from a named const"
    - "Each container's CodeCallout shows its own const string (FLEX_CLASSES or GRID_CLASSES)"
    - "Box colors are defined in a static BOXES array — no dynamic interpolation"
    - "Component is a pure RSC — no 'use client' directive"
    - "Dark mode variants are present on all surfaces and box colors"
  artifacts:
    - path: "src/components/FlexGridComparison.tsx"
      provides: "Flex vs. grid side-by-side RSC comparison with CodeCallout annotations"
      exports: ["default FlexGridComparison"]
    - path: "src/components/FlexGridComparison.tsx"
      provides: "FLEX_CLASSES and GRID_CLASSES single-source consts"
      contains: "const FLEX_CLASSES"
  key_links:
    - from: "FLEX_CLASSES const"
      to: "flex container className AND CodeCallout classes prop"
      via: "single-source reference"
      pattern: "FLEX_CLASSES"
    - from: "GRID_CLASSES const"
      to: "grid container className AND CodeCallout classes prop"
      via: "single-source reference"
      pattern: "GRID_CLASSES"
---

<objective>
Create `src/components/FlexGridComparison.tsx` — a pure RSC rendering two layout containers (flex and grid) side-by-side with identical numbered children and CodeCallout annotations.

Purpose: Teaches the audience when to use flex vs. grid by showing the same three numbered boxes arranged by each layout model, with the class strings visible as callouts below each container.

Output: `src/components/FlexGridComparison.tsx` — pure RSC, no interaction, no page.tsx changes in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-slide-3-core-utility-classes/03-CONTEXT.md
@.planning/phases/03-slide-3-core-utility-classes/03-UI-SPEC.md
@.planning/phases/03-slide-3-core-utility-classes/03-PATTERNS.md

<interfaces>
<!-- Existing components this RSC consumes — read before implementing. -->

From src/components/CodeCallout.tsx:
```typescript
interface CodeCalloutProps {
  classes: string;
}
export default function CodeCallout({ classes }: Readonly<CodeCalloutProps>): JSX.Element
// Renders a monospace chip. Single prop: classes (string). No "use client" needed.
```

Analog import/overline pattern from src/app/what-is-tailwind/page.tsx:
```typescript
import CodeCallout from "@/components/CodeCallout";
// Overline:
<p className="text-xs font-semibold tracking-widest uppercase text-cyan-600 dark:text-cyan-400 3xl:text-base">
  FLEX
</p>
// Callout placement:
<div className="mt-4">
  <CodeCallout classes={SOME_CLASSES} />
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FlexGridComparison RSC</name>
  <files>src/components/FlexGridComparison.tsx</files>

  <read_first>
    - src/app/what-is-tailwind/page.tsx — two-column layout, overline pattern, CodeCallout placement
    - src/components/CodeCallout.tsx — component interface (single `classes: string` prop)
    - .planning/phases/03-slide-3-core-utility-classes/03-UI-SPEC.md — exact box color strings, container wrapper classes, overline copy
  </read_first>

  <action>
Create `src/components/FlexGridComparison.tsx` as a pure RSC (no `"use client"`) with the following exact structure.

**Imports:**
```typescript
import CodeCallout from "@/components/CodeCallout";
```

**Single-source consts (from UI-SPEC — exact strings):**
```typescript
const FLEX_CLASSES = "flex flex-row gap-4";
const GRID_CLASSES = "grid grid-cols-3 gap-4";
```

**Numbered box data array — complete static class strings, no interpolation:**
```typescript
interface BoxConfig {
  num: string;
  className: string;
}

const BOXES: BoxConfig[] = [
  {
    num: "1",
    className:
      "flex items-center justify-center size-16 3xl:size-24 rounded-xl text-xs font-semibold 3xl:text-base bg-violet-200 dark:bg-violet-800 text-violet-900 dark:text-violet-100",
  },
  {
    num: "2",
    className:
      "flex items-center justify-center size-16 3xl:size-24 rounded-xl text-xs font-semibold 3xl:text-base bg-sky-200 dark:bg-sky-800 text-sky-900 dark:text-sky-100",
  },
  {
    num: "3",
    className:
      "flex items-center justify-center size-16 3xl:size-24 rounded-xl text-xs font-semibold 3xl:text-base bg-emerald-200 dark:bg-emerald-800 text-emerald-900 dark:text-emerald-100",
  },
];
```

**Component structure:**
```typescript
export default function FlexGridComparison() {
  return (
    <div className="grid grid-cols-2 gap-6 3xl:gap-12">
      {/* Left column — Flex */}
      <div>
        <p className="text-xs font-semibold tracking-widest uppercase text-cyan-600 dark:text-cyan-400 3xl:text-base">
          FLEX
        </p>
        <div className="mt-4 p-4 bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 rounded-xl 3xl:mt-4 3xl:p-6">
          <div className={FLEX_CLASSES}>
            {BOXES.map((box) => (
              <div key={box.num} className={box.className}>
                {box.num}
              </div>
            ))}
          </div>
        </div>
        <div className="mt-4">
          <CodeCallout classes={FLEX_CLASSES} />
        </div>
      </div>

      {/* Right column — Grid */}
      <div>
        <p className="text-xs font-semibold tracking-widest uppercase text-cyan-600 dark:text-cyan-400 3xl:text-base">
          GRID
        </p>
        <div className="mt-4 p-4 bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 rounded-xl 3xl:mt-4 3xl:p-6">
          <div className={GRID_CLASSES}>
            {BOXES.map((box) => (
              <div key={box.num} className={box.className}>
                {box.num}
              </div>
            ))}
          </div>
        </div>
        <div className="mt-4">
          <CodeCallout classes={GRID_CLASSES} />
        </div>
      </div>
    </div>
  );
}
```

IMPORTANT constraints:
- No `"use client"` — this is a pure RSC.
- FLEX_CLASSES and GRID_CLASSES are complete static string literals — they appear unchanged as both the container's className and the CodeCallout's classes prop.
- BOXES array entries are complete static string literals — no `bg-${color}-200` interpolation anywhere.
- Box numbers (1, 2, 3) are string literals in the BoxConfig — not computed.
  </action>

  <verify>
    <automated>grep -c '"use client"' src/components/FlexGridComparison.tsx || echo "0"</automated>
    <!-- Must output 0 — no use client -->
    <automated>grep -c 'const FLEX_CLASSES' src/components/FlexGridComparison.tsx</automated>
    <automated>grep -c 'const GRID_CLASSES' src/components/FlexGridComparison.tsx</automated>
    <automated>grep -c 'const BOXES' src/components/FlexGridComparison.tsx</automated>
    <automated>grep -rn '\`.*-\${' src/components/FlexGridComparison.tsx | wc -l</automated>
    <!-- Must output 0 — no dynamic interpolation -->
    <automated>yarn build 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - src/components/FlexGridComparison.tsx exists
    - File does NOT contain `"use client"`
    - File contains `const FLEX_CLASSES = "flex flex-row gap-4"`
    - File contains `const GRID_CLASSES = "grid grid-cols-3 gap-4"`
    - File contains `const BOXES` array with 3 entries having complete static className strings
    - FLEX_CLASSES is used as both the flex container's className and in `<CodeCallout classes={FLEX_CLASSES} />`
    - GRID_CLASSES is used as both the grid container's className and in `<CodeCallout classes={GRID_CLASSES} />`
    - File contains `bg-violet-200 dark:bg-violet-800` for box 1 (complete static string)
    - File contains `bg-sky-200 dark:bg-sky-800` for box 2 (complete static string)
    - File contains `bg-emerald-200 dark:bg-emerald-800` for box 3 (complete static string)
    - File does NOT contain any backtick template literal with `${` inside className positions
    - `yarn build` exits 0
  </acceptance_criteria>

  <done>FlexGridComparison.tsx exists as a pure RSC, exports default FlexGridComparison, renders two columns with FLEX_CLASSES and GRID_CLASSES as single-source consts passed to both container elements and CodeCallout, BOXES uses complete static strings for all three box colors, and yarn build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→browser | RSC renders static HTML — no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | FlexGridComparison.tsx | accept | Pure RSC, no user data, no secrets, no async operations. Static presentation content only. |
| T-03-02-02 | Tampering | BOXES array | accept | Static read-only data array, no mutation path. Content is hardcoded presentation copy. |
</threat_model>

<verification>
After task completion:
1. `grep -c '"use client"' src/components/FlexGridComparison.tsx` → must output 0
2. `grep -c 'const FLEX_CLASSES' src/components/FlexGridComparison.tsx` → outputs 1
3. `grep -c 'const GRID_CLASSES' src/components/FlexGridComparison.tsx` → outputs 1
4. `grep -rn '\`.*-\${' src/components/FlexGridComparison.tsx | wc -l` → outputs 0
5. `yarn build` exits 0 — no purge errors
</verification>

<success_criteria>
- FlexGridComparison.tsx is a pure RSC (no "use client")
- FLEX_CLASSES = "flex flex-row gap-4" — single-source, used in both container element and CodeCallout
- GRID_CLASSES = "grid grid-cols-3 gap-4" — single-source, used in both container element and CodeCallout
- BOXES array has 3 entries with complete static class strings (violet, sky, emerald) including dark: variants
- yarn build passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-slide-3-core-utility-classes/03-02-SUMMARY.md`
</output>
