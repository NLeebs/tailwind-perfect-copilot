---
phase: 03-slide-3-core-utility-classes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/CardBuilder.tsx
autonomous: true
requirements:
  - S3-01

must_haves:
  truths:
    - "The card renders at step 0 with only 'w-full max-w-sm' applied — the plain unstyled card"
    - "Clicking a step node jumps the card to that step's cumulative classes"
    - "The CodeCallout shows only the newClasses for the current step (not the full cumulative string)"
    - "Prev button is disabled (opacity-40, cursor-not-allowed) at step 0"
    - "Next button is disabled (opacity-40, cursor-not-allowed) at step 5"
    - "All class strings are complete static literals — no template literals or string concatenation"
    - "Dark mode variants render correctly without prefers-color-scheme"
    - "3xl: escalations are present on all text, spacing, and step node sizes"
  artifacts:
    - path: "src/components/CardBuilder.tsx"
      provides: "Progressive card builder client island with 6-step navigator"
      exports: ["default CardBuilder"]
      contains: "use client"
    - path: "src/components/CardBuilder.tsx"
      provides: "STEPS array with complete static allClasses and newClasses literals"
      contains: "const STEPS"
  key_links:
    - from: "STEPS[activeStep].allClasses"
      to: "card element className"
      via: "direct prop"
      pattern: "STEPS\\[activeStep\\]\\.allClasses"
    - from: "STEPS[activeStep].newClasses"
      to: "CodeCallout classes prop"
      via: "direct prop"
      pattern: "STEPS\\[activeStep\\]\\.newClasses"
---

<objective>
Create `src/components/CardBuilder.tsx` — the "use client" island for the progressive card-building demo.

Purpose: Shows a card evolving through 6 utility categories (Layout → Spacing → Typography → Color → Borders → Flex), with step navigator and CodeCallout showing only the newly added classes at each step. This is the primary interactive demo for the Core Utility Classes slide.

Output: `src/components/CardBuilder.tsx` — self-contained client island, no page.tsx changes in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-slide-3-core-utility-classes/03-CONTEXT.md
@.planning/phases/03-slide-3-core-utility-classes/03-UI-SPEC.md
@.planning/phases/03-slide-3-core-utility-classes/03-PATTERNS.md

<interfaces>
<!-- Existing components this island consumes — read before implementing. -->

From src/components/CodeCallout.tsx:
```typescript
interface CodeCalloutProps {
  classes: string;
}
export default function CodeCallout({ classes }: Readonly<CodeCalloutProps>): JSX.Element
// Renders a monospace chip. Single prop: classes (string).
// Internal styling is fixed — do not override.
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
// clsx + tailwind-merge. Import: import { cn } from "@/lib/utils"
```

Analog structure from src/components/ButtonComparison.tsx:
```typescript
"use client";
import { useState } from "react";
import CodeCallout from "@/components/CodeCallout";
// const SINGLE_SOURCE_CONST = "complete static class string";
// element: className={SINGLE_SOURCE_CONST}
// callout: <CodeCallout classes={SINGLE_SOURCE_CONST} />
```

Analog state pattern from src/components/CssTimeline.tsx:
```typescript
const [expandedIndex, setExpandedIndex] = useState<number | null>(0);
// Apply as: const [activeStep, setActiveStep] = useState(0);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CardBuilder client island</name>
  <files>src/components/CardBuilder.tsx</files>

  <read_first>
    - src/components/ButtonComparison.tsx — single-source const pattern + CodeCallout usage
    - src/components/CssTimeline.tsx — static data array pattern + useState step state
    - src/components/CodeCallout.tsx — component interface (single `classes: string` prop)
    - src/lib/utils.ts — cn() import path and signature
    - .planning/phases/03-slide-3-core-utility-classes/03-UI-SPEC.md — exact class strings for every element
  </read_first>

  <behavior>
    - Test 1: Renders without error — snapshot contains "Tailwind Card" heading and "Utilities compose one step at a time." body
    - Test 2: Initial render shows step 0 classes — card element className equals "w-full max-w-sm" (allClasses for step 0)
    - Test 3: Clicking Next button advances to step 1 — card element className equals "w-full max-w-sm p-6"
    - Test 4: Clicking a step node directly (e.g., node index 3) sets activeStep to 3 — card className equals the step 3 allClasses string
    - Test 5: CodeCallout receives STEPS[activeStep].newClasses — at step 0 it receives "w-full max-w-sm", at step 1 it receives "p-6"
    - Test 6: Prev button is disabled (aria-disabled or disabled prop truthy) at step 0
    - Test 7: Next button is disabled at step 5 (STEPS.length - 1)
  </behavior>

  <action>
Create `src/components/CardBuilder.tsx` with the following exact structure. Do not deviate from the class strings specified below — they come from the approved UI-SPEC.

**File header:**
```typescript
"use client";
import { useState } from "react";
import CodeCallout from "@/components/CodeCallout";
import { cn } from "@/lib/utils";
```

**StepConfig type:**
```typescript
interface StepConfig {
  label: string;
  newClasses: string;
  allClasses: string;
}
```

**STEPS array — all 6 entries with exact class strings from UI-SPEC:**
```typescript
const STEPS: StepConfig[] = [
  {
    label: "Layout",
    newClasses: "w-full max-w-sm",
    allClasses: "w-full max-w-sm",
  },
  {
    label: "Spacing",
    newClasses: "p-6",
    allClasses: "w-full max-w-sm p-6",
  },
  {
    label: "Typo",
    newClasses: "text-sm font-semibold",
    allClasses: "w-full max-w-sm p-6 text-sm font-semibold",
  },
  {
    label: "Color",
    newClasses: "bg-white dark:bg-slate-900 text-slate-900 dark:text-white",
    allClasses:
      "w-full max-w-sm p-6 text-sm font-semibold bg-white dark:bg-slate-900 text-slate-900 dark:text-white",
  },
  {
    label: "Borders",
    newClasses:
      "border border-slate-200 dark:border-slate-800 rounded-2xl shadow-sm",
    allClasses:
      "w-full max-w-sm p-6 text-sm font-semibold bg-white dark:bg-slate-900 text-slate-900 dark:text-white border border-slate-200 dark:border-slate-800 rounded-2xl shadow-sm",
  },
  {
    label: "Flex",
    newClasses: "flex flex-col gap-4",
    allClasses:
      "w-full max-w-sm p-6 text-sm font-semibold bg-white dark:bg-slate-900 text-slate-900 dark:text-white border border-slate-200 dark:border-slate-800 rounded-2xl shadow-sm flex flex-col gap-4",
  },
];
```

**Component structure:**
```typescript
export default function CardBuilder() {
  const [activeStep, setActiveStep] = useState(0);

  return (
    <div>
      {/* Step navigator — full width above the two-column area */}
      <div className="flex items-center justify-between gap-4 mb-8 3xl:mb-12">
        {/* Prev button */}
        <button
          type="button"
          onClick={() => setActiveStep((s) => Math.max(0, s - 1))}
          disabled={activeStep === 0}
          className={cn(
            "flex items-center gap-1 text-sm font-semibold px-4 py-2 rounded-lg border border-slate-200 dark:border-slate-800 bg-white dark:bg-slate-900 text-slate-700 dark:text-slate-300 transition-colors 3xl:text-lg 3xl:px-6 3xl:py-3",
            activeStep === 0 && "opacity-40 cursor-not-allowed pointer-events-none"
          )}
        >
          ◄ Prev
        </button>

        {/* Step nodes row */}
        <div className="flex items-center gap-4">
          {STEPS.map((step, i) => (
            <button
              key={step.label}
              type="button"
              onClick={() => setActiveStep(i)}
              className="flex flex-col items-center gap-1"
            >
              <span
                className={cn(
                  "flex items-center justify-center size-10 rounded-full font-semibold text-sm 3xl:size-16 3xl:text-xl transition-colors",
                  i === activeStep
                    ? "bg-cyan-500 text-white ring-2 ring-cyan-500 ring-offset-2 dark:ring-offset-slate-950 cursor-default"
                    : "bg-slate-200 dark:bg-slate-700 text-slate-500 dark:text-slate-400 cursor-pointer hover:bg-slate-300 dark:hover:bg-slate-600"
                )}
              >
                {i + 1}
              </span>
              <span className="text-xs text-slate-500 dark:text-slate-400 mt-1 3xl:text-sm">
                {step.label}
              </span>
            </button>
          ))}
        </div>

        {/* Next button */}
        <button
          type="button"
          onClick={() => setActiveStep((s) => Math.min(STEPS.length - 1, s + 1))}
          disabled={activeStep === STEPS.length - 1}
          className={cn(
            "flex items-center gap-1 text-sm font-semibold px-4 py-2 rounded-lg border border-slate-200 dark:border-slate-800 bg-white dark:bg-slate-900 text-slate-700 dark:text-slate-300 transition-colors 3xl:text-lg 3xl:px-6 3xl:py-3",
            activeStep === STEPS.length - 1 &&
              "opacity-40 cursor-not-allowed pointer-events-none"
          )}
        >
          Next ►
        </button>
      </div>

      {/* Two-column: card (left) + callout (right) */}
      <div className="grid grid-cols-2 gap-6 3xl:gap-12 mt-8 3xl:mt-12">
        {/* Left: demo card */}
        <div>
          <p className="text-xs font-semibold tracking-widest uppercase text-cyan-600 dark:text-cyan-400 3xl:text-base mb-4">
            RESULT
          </p>
          <div className={STEPS[activeStep].allClasses}>
            <h3>Tailwind Card</h3>
            <p>Utilities compose one step at a time.</p>
          </div>
        </div>

        {/* Right: CodeCallout */}
        <div>
          <p className="text-xs font-semibold tracking-widest uppercase text-cyan-600 dark:text-cyan-400 3xl:text-base mb-4">
            NEW CLASSES
          </p>
          <CodeCallout classes={STEPS[activeStep].newClasses} />
        </div>
      </div>
    </div>
  );
}
```

IMPORTANT constraints:
- Never use template literals or string concatenation to build class strings. Every entry in STEPS must be a complete static string literal exactly as shown above.
- The `cn()` call on step nodes uses a ternary with complete static string branches — not partial strings.
- CardBuilder is a leaf-node island — `"use client"` is ONLY on this file. It will be imported into an RSC page.
- page.tsx does NOT get modified in this task.
  </action>

  <verify>
    <automated>grep -c '"use client"' src/components/CardBuilder.tsx</automated>
    <automated>grep -c 'const STEPS' src/components/CardBuilder.tsx</automated>
    <automated>grep -c 'STEPS\[activeStep\]\.allClasses' src/components/CardBuilder.tsx</automated>
    <automated>grep -c 'STEPS\[activeStep\]\.newClasses' src/components/CardBuilder.tsx</automated>
    <automated>grep -rn '\`.*-\${' src/components/CardBuilder.tsx | wc -l</automated>
    <!-- Last command must output 0 — no dynamic class interpolation -->
    <automated>yarn build 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - src/components/CardBuilder.tsx exists
    - File contains `"use client"` on line 1
    - File contains `const STEPS: StepConfig[]`
    - File contains `useState(0)` for activeStep
    - File contains `STEPS[activeStep].allClasses` applied to the card element className
    - File contains `STEPS[activeStep].newClasses` passed to CodeCallout classes prop
    - File contains `opacity-40 cursor-not-allowed pointer-events-none` on Prev button conditional (disabled at step 0)
    - File contains `opacity-40 cursor-not-allowed pointer-events-none` on Next button conditional (disabled at step 5)
    - File does NOT contain any backtick template literal with `${` inside className or class string positions
    - `yarn build` exits 0
  </acceptance_criteria>

  <done>CardBuilder.tsx exists, exports default CardBuilder, implements all 6 STEPS with exact allClasses/newClasses strings from UI-SPEC, step navigator navigates correctly with boundary disabling, and yarn build passes with no purged classes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→browser | Step navigation interaction — onClick handlers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | CardBuilder step state | accept | State is client-side UI only — no server data, no persistence, no sensitive values. Step index is bounded 0–5 by Math.max/Math.min. |
| T-03-02 | Information Disclosure | CardBuilder.tsx | accept | No secrets, credentials, or user data. All content is static presentation copy. |
| T-03-03 | Denial of Service | onClick handlers | accept | Static island with no async operations, no API calls, no external dependencies beyond React state. |
</threat_model>

<verification>
After task completion:
1. `grep -c '"use client"' src/components/CardBuilder.tsx` → outputs 1
2. `grep -c 'const STEPS' src/components/CardBuilder.tsx` → outputs 1 or more
3. `grep -rn '\`.*-\${' src/components/CardBuilder.tsx | wc -l` → outputs 0 (no dynamic interpolation)
4. `yarn build` exits 0 — no Tailwind class purge errors
</verification>

<success_criteria>
- CardBuilder.tsx is a valid "use client" island with STEPS array containing 6 entries
- All 6 STEPS have complete static allClasses and newClasses string literals matching the UI-SPEC table exactly
- Step navigator renders 6 clickable nodes, Prev/Next buttons, boundary disabling at steps 0 and 5
- CodeCallout receives newClasses (delta only, not cumulative) at each step
- No dynamic class interpolation anywhere in the file
- yarn build passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-slide-3-core-utility-classes/03-01-SUMMARY.md`
</output>
