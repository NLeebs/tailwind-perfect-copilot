---
quick_id: 260506-q01
slug: css-timeline-inline-links
description: Add inline hyperlinks to CssTimeline milestone text
date: 2026-05-06
---

# Quick Task 260506-q01: Add Inline Hyperlinks to CssTimeline Milestone Text

## Goal

Allow milestone strings in `CssTimeline.tsx` to contain clickable inline hyperlinks.
Initial link: "skeuomorphism" → https://www.figma.com/resource-library/what-is-skeuomorphism/
in the "Responsive Revolution" era's milestones.

## Task 1: Extend milestone type and add render helper

**Files:** `src/components/CssTimeline.tsx`

**Action:**
1. Add `MilestoneItem` type: `{ text: string; links: Record<string, string> }`
2. Change `Era.milestones` from `string[]` to `Array<string | MilestoneItem>`
3. Update the responsive revolution era's `"2013: Flat design replaces skeuomorphism..."` milestone to use `MilestoneItem` with the skeuomorphism link
4. Add `renderMilestoneText(item, textColor, darkColor)` helper that splits the string on linked words and wraps matches in styled `<a>` tags
5. Update `TimelineEntry` milestones rendering to call the helper

**Verify:** `yarn build` passes with no type errors; the word "skeuomorphism" appears as a styled link in the responsive revolution era
