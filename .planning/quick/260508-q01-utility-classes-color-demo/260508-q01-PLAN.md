---
phase: quick-260508-q01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ColorPalette.tsx
  - src/app/utility-classes/page.tsx
autonomous: true
requirements: [UTIL-COLORS-01]

must_haves:
  truths:
    - "Built-in Colors section shows a visual swatch grid of Tailwind colors at multiple shades"
    - "Each swatch displays a colored rectangle and its class name (e.g. bg-blue-500)"
    - "No dynamic class interpolation is used — all class strings are complete static strings in a lookup table"
    - "Component is an RSC (no use client directive) — no state or browser APIs needed"
    - "Demo is readable at 1080p and at 1920px (3xl: escalations present)"
  artifacts:
    - path: "src/components/ColorPalette.tsx"
      provides: "Static color swatch grid — array of {name, shades[]} objects, each shade has complete static bg class string"
    - path: "src/app/utility-classes/page.tsx"
      provides: "Built-in Colors section wired to ColorPalette component"
  key_links:
    - from: "src/app/utility-classes/page.tsx"
      to: "src/components/ColorPalette"
      via: "import and render in Built-in Colors section"
      pattern: "import ColorPalette"
---

<objective>
Add a visual color palette swatch grid to the "Built-in Colors" section of Slide 3 (Utility Classes). The section currently has a heading and a link but no demo content.

Purpose: Audience members should leave with a concrete mental model of Tailwind's color system — seeing the actual colors alongside the class names that produce them.
Output: `src/components/ColorPalette.tsx` (new RSC swatch grid) wired into `src/app/utility-classes/page.tsx`.
</objective>

<execution_context>
@/Users/nathanlieberman/Documents/GWRepos/tailwind-perfect-copilot/.planning/quick/260508-q01-utility-classes-color-demo/260508-q01-PLAN.md
</execution_context>

<context>
@/Users/nathanlieberman/Documents/GWRepos/tailwind-perfect-copilot/.planning/STATE.md
@/Users/nathanlieberman/Documents/GWRepos/tailwind-perfect-copilot/src/app/utility-classes/page.tsx
@/Users/nathanlieberman/Documents/GWRepos/tailwind-perfect-copilot/src/components/CodeCallout.tsx
@/Users/nathanlieberman/Documents/GWRepos/tailwind-perfect-copilot/src/components/CardBuilder.tsx

<interfaces>
<!-- CodeCallout: RSC, accepts classes: string, renders monospace code badge -->
<!-- Usage: <CodeCallout classes="bg-blue-500" /> -->

<!-- CardBuilder patterns to follow:
  - Two-column wrapper: grid grid-cols-2 gap-6 3xl:gap-12
  - Overline label: text-xs font-semibold tracking-widest uppercase text-cyan-600 dark:text-cyan-400 3xl:text-base
  - Container: bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 rounded-xl p-4 3xl:p-6
-->

<!-- ColorPalette swatch data shape (use exactly this structure):
interface SwatchRow {
  name: string;        // e.g. "slate", "red"
  shades: SwatchCell[];
}
interface SwatchCell {
  label: string;       // e.g. "500"
  bg: string;          // FULL static class string e.g. "bg-blue-500"
  text: string;        // contrasting text color e.g. "text-white" or "text-slate-900"
}
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ColorPalette RSC component</name>
  <files>src/components/ColorPalette.tsx</files>
  <action>
Create a new Server Component (no "use client"). Define two TypeScript interfaces — SwatchCell and SwatchRow — and a PALETTE constant (array of SwatchRow objects). This is the ONLY source of class strings; no dynamic interpolation anywhere.

Cover these color families: slate, red, orange, amber, yellow, green, teal, blue, violet, pink.
Cover these shades for each: 100, 300, 500, 700, 900.

Each SwatchCell must have:
- `label`: shade number as string (e.g. "100")
- `bg`: complete static Tailwind class string (e.g. "bg-blue-500")
- `text`: contrasting text color — use "text-slate-800" for shades 100/300, "text-white" for shades 500/700/900

Layout: one row per color family, rendered as a labeled row. Each row is a flex container. Each swatch cell is a div with:
- The `bg` class applied via className (direct static string from the data object — acceptable because the full class string is pre-declared in the lookup table, not constructed at render time)
- Fixed dimensions: `h-12 w-16 3xl:h-16 3xl:w-24` (rectangle, not square — landscape to fit more shades)
- Rounded corners: `rounded`
- The shade label centered inside in a small monospace span using the `text` color from the data

Below each row of swatches, show the family name as an overline label using the established pattern:
`text-xs font-semibold tracking-widest uppercase text-slate-500 dark:text-slate-400 3xl:text-sm`

Wrap the whole grid in a container matching CardBuilder's style:
`bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 rounded-xl p-4 3xl:p-6 mt-8 3xl:mt-12`

Add a CodeCallout below the grid showing a representative sample class string to illustrate the naming convention. Use a single static string constant (e.g. `const SAMPLE_CLASS = "bg-blue-500 text-white"`) and pass it to both the CodeCallout and (conceptually) use it as an example — it does not need to wire to a live element. Overline label above the callout: "CLASS FORMAT" using the cyan overline pattern.

IMPORTANT: All bg-* class strings must be listed completely in the PALETTE array. Do not construct "bg-" + color + "-" + shade at render time. The PALETTE array must enumerate every class string explicitly so the Tailwind build scanner can detect them.
  </action>
  <verify>
    <automated>cd /Users/nathanlieberman/Documents/GWRepos/tailwind-perfect-copilot && yarn build 2>&1 | tail -20</automated>
  </verify>
  <done>
    ColorPalette.tsx exists, exports a default RSC, PALETTE array has 10 color families × 5 shades each (50 cells) with complete static class strings, no dynamic interpolation present, component renders swatch grid with CodeCallout.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire ColorPalette into the Built-in Colors section of page.tsx</name>
  <files>src/app/utility-classes/page.tsx</files>
  <action>
Import ColorPalette from "@/components/ColorPalette" at the top of the file (alongside existing imports).

In the "Demo 2 — Tailwind Colors" section (lines 32–47), add the component after the closing `</p>` tag and before the closing `</section>`:

```tsx
<div className="mt-8 3xl:mt-12">
  <ColorPalette />
</div>
```

No other changes to page.tsx. Do not add "use client" — page.tsx must remain an RSC.
  </action>
  <verify>
    <automated>cd /Users/nathanlieberman/Documents/GWRepos/tailwind-perfect-copilot && yarn build 2>&1 | tail -10</automated>
  </verify>
  <done>
    `yarn build` exits 0. The Built-in Colors section now renders ColorPalette. No TypeScript errors. page.tsx has no "use client" directive.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static data → render | PALETTE array is hardcoded; no user input, no external data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q01-01 | Information Disclosure | ColorPalette.tsx | accept | Static RSC with no secrets, no user data, no API calls — read-only presentation component |
</threat_model>

<verification>
1. `yarn build` completes with exit code 0 (no purged classes, no TypeScript errors)
2. Open `localhost:3000/utility-classes` — scroll to "Built-in Colors" section
3. Swatch grid is visible: 10 color rows, 5 shades each, shade labels inside each cell
4. CodeCallout below shows a static class format example
5. No "use client" in ColorPalette.tsx or page.tsx
6. Source contains no dynamic class interpolation (grep confirms: `grep "bg-\${" src/components/ColorPalette.tsx` returns nothing)
</verification>

<success_criteria>
- ColorPalette.tsx created with 10 color families × 5 shades in a static lookup table
- All 50 bg-* class strings are complete static strings (Tailwind build can detect each)
- Built-in Colors section in page.tsx renders the swatch grid
- `yarn build` exits 0
- No dynamic class construction present
- 3xl: breakpoint escalations present on dimensions and text
</success_criteria>

<output>
No SUMMARY.md needed for quick tasks. On completion, confirm `yarn build` passes and the swatch grid is visible at /utility-classes.
</output>
